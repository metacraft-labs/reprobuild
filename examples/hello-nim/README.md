# Hello Nim

Minimal Nim project fixture for local Reprobuild examples and early Nim tool
integration tests.

Expected command shape:

```sh
nim c -r src/hello.nim
```

Expected output:

```text
hello from Nim
```
