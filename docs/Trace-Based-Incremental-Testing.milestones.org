#+TITLE: Trace-Based Incremental Testing for `repro watch` (Prototype)
#+STATUS: Phase 4 COMPLETE except M16. Native LIVE via compile-time instrumentation (M14/M15), dedicated seekable calls.dat stream end-to-end incl. db-backend (M17a/M17b) + all materialized recorders (M20), ct test --incremental standalone (M18), docs (M19) — all DONE, reviewed, pushed. M16 (OS-level Intel PT) is DEFERRED to an x86+PT Linux host (no PT hardware on the arm64-macOS dev host to implement/validate it).

# Phase 1 (M0-M5): the source/interpreted path — DONE.
# Phase 2 (M6-M10): generalize to ALL CodeTracer languages, including the
#   native / Multi-Core-Recorder (MCR) path and Nim's dual recording path.
# Phase 3 (M11-M13): symBodyHash, modern CTFS consumption, live-recording tests.
# Phase 4 (M14-M19): close the gap between the prototype and the spec for native
#   targets and ship the user-facing docs. The prototype's native path used a
#   hand-crafted `native_calltrace.json` and instruction-byte shallow hashing; the
#   spec (Nim-Parallel-Test-Framework §16.7) calls for runtime dependency discovery
#   from a REAL native call trace — Intel PT via RR, OR compile-time instrumentation
#   — and the trace-format spec (codetracer-trace-format-spec/trace-events.md) calls
#   for splitting the call stream (`calls.dat`) from the line-step stream (`steps.dat`)
#   in materialized `.ct` files. Phase 4 implements the testable-here path first
#   (compile-time instrumentation, M14-M15), then the Linux-gated PT/MCR path (M16),
#   the dedicated call stream (M17), the standalone `ct test` integration (M18), and
#   the documentation (M19).

* Overview

  Prototype of the CodeTracer test runner's *runtime-based incremental
  testing* (codetracer-specs `Planned-Features/Nim-Parallel-Test-Framework.md`
  §16.7): **skip re-running a test when none of the functions it previously
  executed have changed.** Dependency discovery comes from a CodeTracer trace
  (the exact set of functions a test executed); a per-function *shallow hash*
  is computed from source; the test's *deep hash* is the combination of its
  executed functions' shallow hashes. If the deep hash is unchanged, the test
  is skipped ("skipped (unchanged)").

  This prototype is integrated with `repro watch`: watching a particular test
  edge with `--ct-incremental` enables the mode, so a file change only re-runs
  the watched test when a function it actually executed changed.

** Home & scope

  - *Code home (prototype):* the **reprobuild** repo — core engine in
    `libs/repro_ct_incremental/`, the watch integration in
    `libs/repro_cli_support` (the `repro watch` command). Chosen for a clean
    Nim build via the dev shell and because `repro watch` lives here.
  - *Trace source:* CodeTracer traces. The prototype reads the **JSON trace
    form** (`trace.json` + `trace_paths.json` + `trace_metadata.json`) with
    `std/json` only — no CTFS binary parsing or `codetracer-trace-format-nim`
    dependency in early milestones (CTFS `.ct` support is a later milestone).
  - *Productionization note:* in production the core would live in the
    CodeTracer `ct test` runner (`codetracer/src/ct_test`); reprobuild would
    consume it. The prototype keeps the same algorithm so the move is
    mechanical.

** Spec anchors

  - §16.1 — concept + the five incremental-testing levels.
  - §16.7.1 — shallow hash (per-function), §16.7.2 — dependency discovery via
    traces, §16.7.3 — deep hash, §16.7.4 — workflow.
  - §16.6 — explicit workflow flags (`--changed-since-catalog`, `--test-list`).
  - Non-determinism: a test marked non-deterministic is always re-run (§16.7,
    lines ~1090/1108).

** Integration points (verified)

  - `repro watch` command: `libs/repro_cli_support/src/repro_cli_support.nim`
    `runWatchCommand` (~13063) → `runDirectWatch` (~13372). Initial build at
    ~13439; watch-path extraction `watchPathsFromReport` at ~5587/13502 — the
    re-run decision hook goes between these.
  - Test edge: `ReproTestFixtureEdge` (~20544): `id`, `selector`, `historyKey`,
    `buildDeps`, `runCmd`, `testName`.
  - Trace records: `Function{path_id, line, name}` and `Call{function_id}` —
    executed functions = the `Function` records referenced by `Call` records.

** Global rules for this campaign

  - No skipped/ignored/weakened tests. Every milestone ships *real* automated
    tests that fail before the implementation and pass after.
  - The shallow/deep hash and skip/re-run decisions are pure and unit-tested in
    isolation; trace reading and watch integration get integration tests.
  - Determinism: hashing must be stable across runs (sort dep sets by name).
  - Each milestone updates this file's checkboxes + the per-milestone STATUS
    line. The implementation agent updates status; the review agent verifies it.

* M0 — Fixtures and trace→executed-functions reader
  :STATUS: complete

  Goal: read a CodeTracer trace and return the exact set of executed functions.

  Deliverables:
  - [X] `libs/repro_ct_incremental/` lib scaffold (nimble + src + tests) wired
    onto `config.nims`'s `--path` lib list.
  - [X] A tiny single-language fixture under
    `libs/repro_ct_incremental/tests/fixtures/m0_three_funcs/`: a ~3-function
    program (`main` calls `used_a` and `used_b`; an `unused_c` exists but is not
    called) and a trace in the JSON form (`trace.json`, `trace_paths.json`,
    `trace_metadata.json`). The fixture trace MUST match the real codetracer
    JSON schema — derive it by inspecting a real trace
    (`codetracer-example-recordings/` or by recording the fixture with a
    recorder); document the schema you confirmed in a fixture README.
  - [X] `ExecutedFunction = object {name: string; file: string; defLine: int}`.
  - [X] `proc readExecutedFunctions*(traceDir: string): Result[seq[ExecutedFunction], string]`
    — parses the trace, resolves `Call.function_id → Function.name/path/line`,
    returns the de-duplicated, name-sorted executed set.

  Automated tests:
  - test_name: reads_executed_functions_from_fixture_trace
    description: Over the m0 fixture, `readExecutedFunctions` returns exactly
    {main, used_a, used_b} with correct file + defLine; `unused_c` is absent.
  - test_name: malformed_trace_returns_err_not_crash
    description: A truncated/garbage trace dir yields an `Err`, never a crash.
  - test_name: empty_call_stream_returns_empty_set
    description: A trace with `Function` records but no `Call` records returns
    an empty executed set (defensive).

  Acceptance: the reader works on the real JSON schema (not an invented one);
  the fixture README cites the real trace it was modeled on.

* M1 — Deep-hash invalidation engine (the core)
  :STATUS: complete

  Goal: decide skip vs re-run from executed functions + current source.

  Deliverables:
  - [X] `proc shallowHash*(funcSource: string): string` — stable hash of a
    function's body text (normalize trailing whitespace; document what is/ isn't
    significant). Source extraction: given `ExecutedFunction{file, defLine}`,
    extract the function body via a simple, documented indentation/brace
    heuristic adequate for the fixture language (tree-sitter is a later
    milestone).
  - [X] `proc deepHash*(funcs: seq[(name, shallow)]): string` — sort by name,
    hash the concatenation (§16.7.3).
  - [X] A cache type + JSON persistence: `test → {deepHash, deps:[ExecutedFunction]}`
    stored under `.repro-ct-incremental/cache.json` (path configurable).
  - [X] `proc decide*(testId; traceDir; sourceRoot; cache): IncrementalDecision`
    returning `idRunFresh` (no cache / new test), `idSkipUnchanged`, or
    `idRerunChanged{changedFuncs}`. On a fresh/changed run the caller is expected
    to re-execute, re-trace, and call `record*` to update the cache.
  - [X] `proc record*(cache; testId; traceDir; sourceRoot)` — recompute deps +
    deep hash from a fresh trace and store them.

  Automated tests (the "simple integration test, few functions"):
  - test_name: unchanged_source_skips
    description: record() over the m0 fixture, then decide() with identical
    source ⇒ `idSkipUnchanged`.
  - test_name: changing_an_executed_function_reruns
    description: After record(), edit `used_a`'s body ⇒ decide() ⇒
    `idRerunChanged` listing `used_a`.
  - test_name: changing_an_unexecuted_function_skips
    description: After record(), edit `unused_c` (never executed) ⇒ decide() ⇒
    `idSkipUnchanged` (proves function-level, not file-level, precision).
  - test_name: new_test_without_cache_runs_fresh
    description: decide() for a test absent from the cache ⇒ `idRunFresh`.
  - test_name: deep_hash_is_order_independent_and_stable
    description: deepHash is identical regardless of dep input order and stable
    across calls.
  - test_name: removed_executed_function_reruns
    description: If a dep function is deleted from source after record(), decide()
    ⇒ `idRerunChanged` (missing-dep treated as changed, never silently skipped).

  Acceptance: function-level precision is demonstrated by
  `changing_an_unexecuted_function_skips` passing alongside
  `changing_an_executed_function_reruns`.

* M2 — `repro watch --ct-incremental` integration
  :STATUS: complete

  Goal: wire the engine into `repro watch` for a single watched test edge.

  Deliverables:
  - [X] New `repro watch` flags: `--ct-incremental` (enable the mode) and
    `--ct-incremental-trace-dir=PATH` (where the test's trace is produced;
    prototype assumption — a fixed dir the test writes its trace to). Parsed
    alongside the existing watch flags via the exported, unit-tested
    `applyWatchCtIncrementalFlag` seam (the watch argument loop delegates to it).
  - [X] A pure decision seam in the engine lib —
    `repro_ct_incremental/watch.nim`'s
    `watchTestEdgeDecision(testId, traceDir, sourceRoot, cachePath):
    WatchEdgeDecision` — loads the cache and calls `decide`, returning a
    skip/run verdict + reason. This is what the M2 skip/re-run tests drive
    directly (no live wall-clock filesystem race).
  - [X] A watch decision hook in `runDirectWatch`: when `--ct-incremental` is
    set and the target is the watched test edge, on each rebuild cycle (cycle >
    1) it calls the seam and, when the verdict is skip, reports
    "skipped (unchanged: <test>)" and does NOT re-run the test edge — it resumes
    watching; otherwise it re-runs and `record()`s the fresh trace into the
    cache. Cycle 1 (initial build) always runs + records a baseline. With the
    flag absent the path is byte-for-byte the legacy behaviour.
  - [X] An `eventSink`/report line (`ct-incremental` watch events: skip / rerun
    / record-error) so the skip vs re-run decision is observable in the watch
    output and assertable.

  Automated tests:
  - [X] test_name: watch_ct_incremental_flag_parses
    description: `repro watch --ct-incremental --ct-incremental-trace-dir=…` is
    accepted; absence leaves watch behaviour unchanged.
    (`libs/repro_cli_support/tests/t_watch_ct_incremental_flags.nim`, drives the
    real parser via `applyWatchCtIncrementalFlag`.)
  - [X] test_name: watch_skips_test_edge_when_executed_funcs_unchanged
    description: Drive one watch cycle (a change to an unexecuted function) ⇒ the
    watched test edge is reported skipped, not re-run.
    (`libs/repro_ct_incremental/tests/t_watch_seam.nim`, via the seam.)
  - [X] test_name: watch_reruns_test_edge_when_executed_func_changed
    description: Drive one watch cycle (a change to an executed function) ⇒ the
    watched test edge is re-run. (`t_watch_seam.nim`, via the seam.)
  - [X] test_name: watch_without_flag_is_unchanged
    description: Without `--ct-incremental`, the watch path/run behaviour is
    byte-for-byte the legacy behaviour (no regression).
    (`t_watch_ct_incremental_flags.nim`: with neither flag the parser consumes
    nothing and the parsed state is the legacy default.)

  Acceptance: the two skip/re-run watch tests pass deterministically (drive the
  decision through a seam, not a live wall-clock filesystem race).

* M3 — Second and third languages
  :STATUS: complete

  Goal: prove language-agnosticism. Add fixtures + per-language function-body
  extraction for two more languages whose syntax exercises genuinely different
  extraction strategies (Python — indentation; JavaScript — braces) alongside
  the M1 Ruby language.

  Deliverables:
  - [X] Per-language source extraction strategy abstraction
    (`FunctionBodyExtractor`) selected by file extension —
    `libs/repro_ct_incremental/src/repro_ct_incremental/extractors.nim`: a
    registry mapping `.rb`/`.py`/`.js` → an `ExtractorProc` returning
    `Result[string, string]`. Ruby + Python share an indentation extractor;
    JavaScript uses a brace-matching extractor (skips braces in strings and
    `//` / `/* */` comments — documented limitation: not a full JS lexer). An
    unknown extension ⇒ a clear `Err` (`extractorFor`), which the engine maps
    to the `"missing"` shallow hash so the test fail-safes to a re-run — never
    a silent wrong hash. The M1 Ruby extraction was refactored behind this
    abstraction; `engine.nim` now contains ZERO per-language branching (it
    calls `extractFunctionBody(dep.file, lines, defLine)`).
  - [X] One trace fixture per new language in the SAME real CodeTracer JSON
    schema M0 uses — `tests/fixtures/m3_python_funcs/` and
    `tests/fixtures/m3_js_funcs/` (each: `main` calls `used_a`+`used_b`;
    `unused_c` defined-but-uncalled). Traces are hand-crafted in the
    M0-confirmed real schema; live-recording validation with the real Python/JS
    recorders is deferred (the recorders are not prebuilt in this dev env — see
    each fixture README, which also documents the re-record command). The same
    M1 `decide()`/`record()` engine drives all three with no per-language
    branching outside the extractor.

  Automated tests (`tests/t_multilang_extractors.nim`):
  - [X] test_name: <lang>_executed_function_change_reruns (python + javascript)
  - [X] test_name: <lang>_unexecuted_function_change_skips (python + javascript)
  - [X] test_name: extractor_selected_by_extension
    description: The correct extractor is chosen per `.rb`/`.py`/`.js` (and an
    unknown extension yields a clear, asserted error, never a silent wrong
    hash); the .py (indentation) and .js (braces) extractors are driven over
    snippets to confirm genuinely different strategies.

  Acceptance: identical engine, three languages, all skip/re-run tests green.

* M4 — Larger programs and transitive dependencies
  :STATUS: complete

  Goal: scale to many functions, transitive call chains, and multiple test
  edges; demonstrate the "unchanged transitive dependency graph ⇒ skip" case.

  Deliverables:
  - [X] A larger fixture (≥15 functions, ≥3 call levels, ≥3 tests with
    overlapping and disjoint dependency sets).
    Implemented as `tests/fixtures/m4_large_python/` — a Python program with
    18 functions, a depth-4 call graph (`run_a→mid_a→helper_one→leaf_deep`),
    and three per-test traces (`trace_a/`, `trace_b/`, `trace_c/`) whose Call
    streams encode the executed sets documented in the fixture README. The
    `.py` indentation extractor is used (no JavaScript here).
  - [X] Per-test dependency precision: editing a leaf function re-runs exactly
    the tests that executed it, and skips the rest.
    Driven by `recordAll` in `t_large_program_precision.nim`, which records all
    three tests into ONE cache; editing `leaf_shared` (in A and B) re-runs only
    A and B, editing `leaf_a_only` (in A only) re-runs only A — verified with
    function-level changed-set assertions and no false skips.
  - [X] Cache pruning of removed tests/functions across runs.
    `engine.pruneCache(cache, liveTestIds)` removes entries for tests absent
    from the live set and persists the result; removed FUNCTIONS within a
    still-present test are already handled by M1 (missing ⇒ "missing" hash ⇒
    re-run + re-record).

  Automated tests (`tests/t_large_program_precision.nim`, all driving the
  three `m4_large_python` tests through ONE cache):
  - [X] test_name: editing_shared_leaf_reruns_only_dependent_tests
    description: Edit `leaf_shared` (executed by test_a + test_b, not test_c)
    ⇒ A and B `idRerunChanged` (changed-set == `@["leaf_shared"]`), C
    `idSkipUnchanged`.
  - [X] test_name: editing_disjoint_function_skips_unrelated_tests
    description: Edit `leaf_a_only` (executed by only test_a) ⇒ A reruns, B
    and C skip.
  - [X] test_name: transitive_callee_change_reruns_caller_test
    description: A test that executed `a→b→c` re-runs when `c` changes, because
    `c` is in its executed set (runtime deps are transitive by construction).
    Here `run_a→mid_a→helper_one→leaf_deep` (depth 4): editing `leaf_deep`
    re-runs only test_a; B and C skip. The precondition (`leaf_deep` ∈ test_a's
    recorded set, ∉ B/C) is asserted directly from the trace.
  - [X] test_name: removed_test_is_pruned_from_cache
    description: After recording 3 tests, `pruneCache(cache, [A, B])` removes
    C's entry from the in-memory AND persisted cache; a subsequent decide for C
    ⇒ `idRunFresh`. Pruning is idempotent. Survivors A/B still skip.

  Supporting guard tests (same file): `fixture_executed_sets_match_readme`
  (the committed traces encode exactly the documented per-test sets, and
  `dead_code`/`unused_helper` are in no set), `all_tests_skip_when_source_
  unchanged` (the unchanged-graph ⇒ skip baseline across all three tests), and
  `editing_dead_code_reruns_nothing`.

  Acceptance: selective re-run across a multi-test fixture with no false skips.

* M5 — Robustness and correctness guards
  :STATUS: complete

  Goal: never skip a test that should run.

  Deliverables:
  - [X] `deterministic`/non-deterministic marking: tests marked
    non-deterministic are always re-run regardless of hashes (§16.7).
    `CachedTest.deterministic` (defaults true) is persisted in the cache JSON,
    settable at `record` time (`deterministic = false`) or via the separate
    `markNonDeterministic` API. `decide` checks it FIRST (before any hashing) and
    returns the dedicated `idRerunNonDeterministic`; `watch.nim` maps it to a
    `weaRun` with reason ``non-deterministic``.
  - [X] Conservative fallbacks: missing trace, unreadable source, hash-tooling
    error ⇒ re-run (fail-safe), with a diagnostic; never a silent skip.
    `decide` now probes the trace dir (`traceDirReadable`) and returns the new
    `idRerunFailSafe{reason}` on a missing/unreadable trace dir or required trace
    file. An unreadable source file / extraction / hashing error already maps to
    the reserved `"missing"` shallow hash ⇒ `idRerunChanged` (M1, confirmed and
    extended for whole-file deletion). `isRerun` centralises "skip iff
    `idSkipUnchanged`" so no caller can mistake a new kind for a skip.
  - [X] Cache schema versioning: a cache from an older schema is ignored
    (re-run), not mis-parsed. `CacheVersion = 2`; `loadCache` returns a FRESH
    (empty) Ok cache when the on-disk `version` is absent/non-int or ≠
    `CacheVersion` (older OR newer), so every test re-runs `idRunFresh`.
  - [X] Concurrent/independent watched edges keep separate cache entries.
    Entries key by test id; `independent_watched_edges_keep_separate_cache_
    entries` proves two edges coexist in one cache file and are invalidated
    independently (marking edge A non-deterministic does not affect edge B).

  Automated tests (`tests/t_robustness.nim`):
  - [X] test_name: nondeterministic_test_always_reruns
  - [X] test_name: missing_trace_falls_back_to_rerun
  - [X] test_name: unreadable_source_falls_back_to_rerun
  - [X] test_name: stale_cache_schema_is_ignored_and_reruns
  - [X] test_name: full_suite_has_no_regressions
    description: Scoped to what the feature touches (the whole-project `.#test`
    is BLOCKED by an unrelated pre-existing `undeclared identifier: 'Executable'`
    error in `repro_project_dsl/macros_b.nim` from commit `2dbd9111` — NOT a
    campaign regression, documented in the test file header). Part (a):
    `noflag_watch_decision_is_legacy_default` asserts in code that with
    `--ct-incremental` absent the watch decision is the legacy run path (a
    disabled `WatchCtIncrementalGate` via `gatedWatchDecision` never consults the
    engine and never skips). Part (b): verification re-runs ALL M0–M4 lib tests +
    `repro_cli_support/tests/t_watch_ct_incremental_flags.nim` + standalone
    `nim check libs/repro_cli_support/src/repro_cli_support.nim` (exit 0) — all
    green.

  Acceptance: every fail-safe path re-runs; no path can silently skip a test
  that should run. The ONLY skip path is `idSkipUnchanged`, reachable only when
  the test is deterministic, its trace dir is readable, and every recorded
  dependency's current shallow hash equals its recorded hash.

* ============================================================================
* PHASE 2 — All CodeTracer languages, including native / MCR
* ============================================================================

  Phase 1 implemented exactly ONE of CodeTracer's two incremental-testing
  mechanisms (§16.7): the *source/interpreted* path — dependency discovery from
  canonical `Function`/`Call` trace records + a *source-text* shallow hash.
  CodeTracer also records **native** programs (C, C++, Rust, Go, Pascal, D,
  Fortran, Ada, Crystal, Odin, V, and Nim compiled via C) through the
  Multi-Core Recorder (MCR) / RR + DWARF. Native traces do **not** emit
  `Function`/`Call` records, and a function's identity is its *compiled
  instruction bytes*, not its source text (spec §16.7.1 "System languages
  (DWARF-based)" + native-recorder `AUDIT-CTFS-2026-05.md`).

  So the two mechanisms are independent on BOTH axes:

  | path                | dependency discovery                 | shallow hash                 |
  |---------------------|--------------------------------------|------------------------------|
  | source/interpreted  | canonical `Function`/`Call` records  | source text (Phase 1 extractors) |
  | native / MCR        | executed-function set from the native trace's calltrace (MCR emulator CallRecords / Intel-PT+DWARF) | function's compiled instruction bytes via the binary's symbol/DWARF table |

  **Nim is special**: it is recordable BOTH ways — materialized Nim-source
  tracing (source path) AND compiled-via-C MCR tracing (native path). The
  engine must pick the strategy *per trace*, by detecting the backend.

  Phase 2 generalizes the engine into pluggable `(DependencyDiscovery,
  ShallowHash)` strategies selected by a detected `TraceBackend`, then adds the
  native path, the Nim dual path, and the full language→strategy matrix. Native
  shallow hashing uses real compiled fixtures (a tiny C/Rust program built with
  the toolchain in the dev shell) so instruction-byte hashing is exercised for
  real; native *traces* (the executed-function set) are hand-crafted in the real
  native-trace shape where a live MCR/RR run isn't available in the dev shell
  (documented per fixture, mirroring Phase 1). The same fail-safe invariant
  holds: any ambiguity ⇒ re-run, never a silent skip.

* M6 — Backend abstraction + detection
  :STATUS: complete

  Goal: refactor the engine so dependency discovery and shallow hashing are
  pluggable strategies chosen by the detected trace backend — with NO behaviour
  change for the existing source/interpreted languages.

  Deliverables:
  - [X] `TraceBackend` enum: `tbSourceInterpreted` (canonical Function/Call +
    source hash), `tbNativeDwarf` (native MCR/RR + instruction-byte hash),
    `tbNimInstrumented` (reserved for Nim's future materialized path).
    Implemented + documented in
    `libs/repro_ct_incremental/src/repro_ct_incremental/backends.nim`.
  - [X] `proc detectBackend*(traceDir: string): Result[TraceBackend, string]` —
    detect from trace shape/metadata: a `trace.json` (canonical records) ⇒
    source/interpreted; an `rr/` dir, a `.ct` CTFS container, OR a
    `trace_db_metadata.json` ⇒ native; ambiguous (both shapes)/unknown/empty ⇒
    `Err` (⇒ re-run upstream). An explicit `recorder_backend` metadata field
    (`rr`/`mcr`/`ttd` ⇒ native, `interpreter` ⇒ source, `nim-instrumented` ⇒ the
    reserved Nim path) is honoured OVER structure; an explicit-but-unrecognised
    value fails safe (`Err`). Precedence (explicit metadata > structure) is
    documented on `detectBackend`.
  - [X] Generalize the engine: a `DependencyDiscovery` seam (the current JSON
    reader `readExecutedFunctions` is the source-interpreted impl) and a
    `ShallowHasher` seam (the extractor-based `shallowHashOfDepSource` is the
    source impl), both objects-of-procs selected by `backendStrategies(backend)`.
    `decide`/`record` detect the backend then route through its seams. For
    `tbNativeDwarf`/`tbNimInstrumented` (nil seams until M7-M9) `record` returns a
    "backend not yet supported" `Err` and `decide` returns `idRerunFailSafe` with
    that reason — a guaranteed re-run, NEVER a skip. The source path is
    byte-for-byte identical (same cache format, same hashes, same decisions).

  Automated tests (`tests/t_backend_abstraction.nim`; fixtures under
  `tests/fixtures/m6_backends/`):
  - [X] test_name: detect_backend_source_vs_native
    description: a canonical `trace.json` fixture ⇒ `tbSourceInterpreted`; an
    `rr/`-dir, a `.ct`-container, and a `trace_db_metadata.json` fixture ⇒
    `tbNativeDwarf`; an empty dir and a both-signals dir ⇒ `Err`.
  - [X] test_name: source_path_unchanged_through_abstraction
    description: the Phase-1 skip/rerun/fresh decisions over the real m0 fixture
    yield the IDENTICAL result through the seams; a companion
    `source_path_hashes_byte_for_byte_identical` asserts the recorded deep hash +
    per-dep shallow hashes are unchanged (not just the decision).
  - [X] test_name: backend_metadata_field_overrides_structure
    description: a `trace.json` + `recorder_backend: "rr"` ⇒ native; an `rr/`
    subdir + `recorder_backend: "interpreter"` ⇒ source.
  - [X] Guards: `unimplemented_native_backend_reruns_never_skips` (native/Nim
    seams unimplemented ⇒ `record` Errs, `decide` ⇒ `idRerunFailSafe`, never a
    skip) and `ambiguous_trace_decide_is_failsafe_rerun`.

  Acceptance: all Phase-1 tests pass unchanged (verified: the 7 lib suites +
  `repro_cli_support` flag test + `nim check libs/repro_cli_support/...` exit 0);
  the seams exist and are selected by `detectBackend`.

* M7 — Native shallow hash (compiled instruction bytes)
  :STATUS: complete

  Goal: hash a native function by its compiled instruction bytes (§16.7.1), for
  real, against a compiled binary.

  Deliverables:
  - [X] A real native fixture: a tiny C program (4 functions — `used_a`,
    `used_b`, `unused_c`, `main`; `used_a`/`used_b` on the test path, `unused_c`
    off it) compiled WITH symbols in the dev shell. Source + `build.sh` committed
    under `tests/fixtures/m7_native_c/`; the binary is NOT committed — the test
    builds it (and edited copies) at test time into temp dirs, so rebuild
    determinism and per-function precision are genuinely exercised. Non-`main`
    functions are `noinline` leaves so each keeps a distinct, position-independent
    block of machine code.
  - [X] `proc nativeFunctionTable*(binary: string): Result[Table[string, HashSlice], string]`
    — `function_name → (file-offset,size)` from the binary's symbol table, in
    `src/repro_ct_incremental/native_hash.nim` (re-exported from the package).
    PLATFORM: this host is arm64 macOS ⇒ Mach-O. `nm --print-size` is zero on
    Mach-O, so sizes come from the delta to the next symbol's address (symbols
    address-sorted via `nm -n --defined-only`), bounding the last function by the
    end of `__TEXT,__text`. The section's `addr`/`offset`/`size` are parsed from
    `otool -l`; the addr→file-offset mapping is
    `file_offset = sym.vmaddr − text.addr + text.offset`. A documented ELF branch
    (`objdump --section-headers` + the same mapping) is compiled for Linux CI.
  - [X] `proc shallowHashNative*(binary, funcName): Result[string, string]` —
    hashes the function's instruction bytes `binary[offset ..< offset+size]`
    (read from the file) via `std/hashes` (same representation as
    `engine.shallowHash`). Missing function / zero-or-negative size / unreadable
    binary / failing tool ⇒ `Err` (⇒ re-run upstream, never skip).

  Automated tests (`tests/t_native_hash.nim`; build a real binary, edit C
  source, rebuild):
  - [X] test_name: native_function_hash_is_stable_across_identical_rebuilds
    description: two builds of identical source yield the same per-function
    instruction-byte hash. (Stable because only the function's `__text` bytes are
    hashed; the full binaries differ only in DWARF/build-path metadata after the
    text section — verified: all byte diffs land past the `__text` end.)
  - [X] test_name: editing_a_native_function_changes_its_instruction_hash
    description: edit `used_a`'s body (`return 1` → a loop), rebuild ⇒ `used_a`'s
    instruction-byte hash changes.
  - [X] test_name: editing_one_native_function_leaves_anothers_hash_stable
    description: editing `used_a` does NOT change `used_b`'s or `unused_c`'s
    instruction-byte hash — the table locates each function independently by its
    own symbol and hashes only that function's bytes, so an edit confined to
    `used_a` cannot leak into a sibling's hash. (Asserts `used_a`'s hash DID
    change first, so stability is meaningful.) NOTE: on this host growing
    `used_a` relocates it toward a LOWER address while `used_b`/`unused_c`/`main`
    keep theirs, so this test does not itself exercise a sibling whose ADDRESS
    shifts — the genuine relocation cases are the two tests below.

  Relocation precision — the HONEST per-function-precision picture (the property
  is NOT unconditional, and the code+tests+docs now say so):
  - [X] test_name: relocated_leaf_function_keeps_its_instruction_hash
    description: a layout where an unedited POSITION-INDEPENDENT leaf (`callee`)
    genuinely relocates (a `gap` function is inserted before its caller) keeps
    its instruction-byte hash — position-independent bytes are relocation-
    invariant. Asserts the leaf's file offset actually moved, so stability is not
    vacuous.
  - [X] test_name: relocated_call_containing_function_rehashes_safely
    description: an unedited CALL-CONTAINING function (`caller_d`, which `bl`s
    `callee`) whose caller→callee DISTANCE changes (the inserted `gap` widens it)
    has DIFFERENT instruction bytes — the pc-relative `bl` operand changes
    (empirically `0x97fffff2 → 0x97ffffd5`) — so its hash CHANGES despite
    unchanged source. This is the DOCUMENTED LIMITATION. It is SAFE: a changed
    shallow hash drives the engine to a conservative re-run (`idRerunChanged`),
    never a false skip — a precision/usefulness limitation, never a correctness
    hole. Documented in `native_hash.nim`, the fixture README, and here. A future
    milestone could normalize pc-relative operands to recover precision; M7 does
    not.
  - [X] test_name: missing_or_zero_size_function_errs
    description: a nonexistent function name, a missing/unreadable binary, and the
    zero/negative-size contract (every emitted slice has positive size; the only
    way to reach a non-positive slice is an absent name, which Errs) ⇒ `Err`,
    never a usable hash.
  - [X] supporting guard: native_function_table_locates_each_fixture_function —
    the table contains all four fixture functions with positive-size slices.

  Acceptance: real per-function instruction-byte hashing with per-function
  precision for position-independent code, a fail-safe `Err` on any lookup
  problem, and an HONESTLY DOCUMENTED + TESTED relocation limitation
  (call-containing functions whose callee distance shifts conservatively
  re-hash ⇒ a SAFE re-run, never a false skip).

* M8 — Native dependency discovery + native incremental decision
  :STATUS: complete

  Goal: end-to-end native incremental testing — skip a native test when none of
  the native functions it executed changed at the instruction-byte level.

  Deliverables:
  - [X] `proc readExecutedFunctionsNative*(traceDir): Result[seq[ExecutedFunction], string]`
    — the native `DependencyDiscovery` impl in
    `src/repro_ct_incremental/native_trace.nim`: reads the executed-function set
    from a native trace's calltrace (`<traceDir>/native_calltrace.json`). The
    fixture trace is hand-crafted (no live MCR/RR run in the dev shell, mirroring
    Phase 1) in a JSON shape modeled on the real native-recorder calltrace —
    `codetracer-native-recorder/ct_emulator/src/ct_emulator/`:
    `write_log.nim`'s `CallRecord {tickEnter,tickExit,callerPc,calleePc}`,
    `calltrace_collector.nim` (MCR/RR collection), and `calltrace.nim`'s
    `buildCallTree`→`CallNode {functionName,sourceFile,sourceLine,calleePc}`
    (PCs resolved to names via `source_index.resolvePCs`/addr2line over the ELF).
    The JSON is a documented prototype projection (`binary` ⇐ the recorder's
    `elfPath`; `calls[].functionName`/`calleePc` ⇐ `CallNode`'s fields); there is
    no canonical on-disk JSON for the calltrace upstream, so the shape + mapping
    are documented in `native_trace.nim` and the fixture README. The returned
    `ExecutedFunction` carries the function NAME and, in `.file`, the BINARY path
    (native deps key on name+binary, not a source line; `defLine` always 0) —
    convention documented in `native_trace.nim`.
  - [X] Native backend wired through the M6 seams: `backendStrategies(tbNativeDwarf)`
    returns a real pair — discovery = `readExecutedFunctionsNative`, hasher =
    `shallowHashOfDepNative` calling `shallowHashNative(dep.file (=binary),
    dep.name)`. The native hasher IGNORES `sourceRoot` (deps carry the binary in
    `.file`), so the source path is NEVER touched on the native route. The cache
    stores the same `{deepHash, deps}` shape — the binary travels in the slot a
    source path would, no schema change. `decide`'s readability/backend guards are
    now backend-dispatched (backend detection runs FIRST; native traces probe
    `native_calltrace.json`).
  - [X] `record` hashes native deps against the CURRENT binary (the trace's
    `binary`); `decide` rebinds the cached native deps to the CURRENT trace's
    binary (`currentDepLocations`, the native analogue of the source path's
    "re-hash cached deps under the current sourceRoot") and skips iff all
    unchanged. A missing/unreadable binary, a function absent from the symbol
    table, or any native-hash `Err` ⇒ the reserved `"missing"` shallow hash ⇒
    re-run, never a skip.

  Automated tests (`tests/t_native_decision.nim`; fixture
  `tests/fixtures/m8_native_c/`; real compiled binary built at test time +
  hand-crafted native calltrace pointing at it):
  - [X] test_name: native_unchanged_binary_skips
    description: record() then decide() with the identical rebuilt binary ⇒
    `idSkipUnchanged`.
  - [X] test_name: native_changing_an_executed_function_reruns
    description: edit + rebuild an EXECUTED native function (`used_a`) ⇒
    `idRerunChanged` naming it (and NOT the unaffected sibling `used_b`).
  - [X] test_name: native_changing_an_unexecuted_function_skips
    description: edit + rebuild a native function the test did NOT execute
    (`unused_c`) ⇒ `idSkipUnchanged` (function-level precision at the machine-code
    level). A guard asserts the executed leaves' instruction-byte hashes are
    GENUINELY unchanged across the edit (and unused_c's OWN hash DID change), so
    the skip is real, not a coincidental re-run.
  - [X] test_name: native_missing_binary_falls_back_to_rerun
    description: a missing/unreadable binary ⇒ `idRerunChanged` (deps hash to
    "missing") — re-run, never skip.
  - [X] supporting guards: `native_executed_set_matches_fixture_calltrace`
    (reader returns exactly {used_a,used_b}, file=binary, defLine=0),
    `native_detect_routes_through_native_backend`, and
    `native_missing_calltrace_file_fails_safe` (absent calltrace ⇒
    `idRerunFailSafe` at decide, `Err` at record).

  Native fixture layout (the skip-genuineness argument, empirically confirmed):
  the executed set is exactly the two pure POSITION-INDEPENDENT leaves
  {`used_a`,`used_b`} (main, the only call-containing function, is deliberately
  excluded), so editing the unexecuted `unused_c` cannot change any executed
  function's instruction bytes. Verified on this host (arm64 macOS): the unused_c
  edit relocates used_a/used_b to lower addresses yet leaves their hashes
  byte-identical (used_a `5b33e421faa53965`, used_b `69d8b6c436ed0ff9` before and
  after) while unused_c's own hash changes — so
  `native_changing_an_unexecuted_function_skips` truly skips.

  Acceptance: native skip/rerun precision demonstrated end-to-end on a real
  compiled binary; `native_changing_an_unexecuted_function_skips` passes
  alongside `native_changing_an_executed_function_reruns`. No native FALSE-SKIP
  is possible: the only skip path is `idSkipUnchanged`, reached only when the
  test is deterministic, the native calltrace is readable, the backend detects
  native, and every executed function's CURRENT instruction-byte hash equals its
  recorded hash; any binary/lookup error maps to the "missing" sentinel ⇒
  re-run, and a relocated call-containing function (the M7 limitation) re-hashes
  to a conservative re-run, never a skip.

* M9 — Nim's dual recording path
  :STATUS: complete

  Goal: the SAME Nim program decides correctly whether it was traced via the
  materialized source path (source-text hash) or the native MCR path
  (instruction-byte hash), chosen per trace by backend detection.

  Deliverables:
  - [X] A `.nim` SOURCE extractor added to the M3 `FunctionBodyExtractor`
    registry (`extractors.nim`): Nim routines (`proc`/`func`/`method`/`template`/
    `macro`) are indentation-delimited, so `.nim` reuses the SAME
    `extractByIndentation` strategy as Ruby/Python — body = the `proc` line
    through the next non-blank line at indentation `<= the proc's indent`. The
    rule is documented in `extractors.nim`; an unknown extension still Errs
    (M3 fail-safe). `t_multilang_extractors.nim`'s
    `extractor_selected_by_extension` now also asserts `.nim` resolves and
    applies the indentation rule.
  - [X] A Nim fixture program recordable two ways under
    `tests/fixtures/m9_nim_dual/`:
      * SOURCE: `src/calc.nim` (4 procs; `usedA`/`usedB`/`mainCalc` executed,
        `unusedC` not) + `trace_source/` in the canonical Function/Call JSON
        shape (⇒ `tbSourceInterpreted` ⇒ `.nim` source extractor over calc.nim;
        explicit `recorder_backend: "interpreter"`). A guard test
        (`source_trace_deflines_match_calc_nim`) pins the trace defLines to the
        actual `proc` lines so a doc edit can't silently break extraction.
      * NATIVE: the test COMPILES the same `calc.nim` with `nim c` (via
        `build.sh`, leaf-stabilising flags) at test time, and `trace_native/`
        carries a native structural signal (`trace_db_metadata.json`,
        `recorder_backend: "rr"`) + a native calltrace whose `functionName`s are
        the binary's actual MANGLED symbols, DISCOVERED by running `nm` on the
        freshly-built binary (`t_nim_dual.nim`'s `mangledName`). Documented in the
        fixture README + `build.sh`: with `-g` the dev-shell Nim emits the
        Itanium-style `_ZN<len>calc<len>usedAE`; without `-g`,
        `usedA__<modulehash>_uN` (build-specific suffix) — so names MUST come from
        the binary, never the source identifier.
  - [X] The engine selects the shallow-hash strategy per trace via
    `detectBackend` — a Nim source-traced run uses the `.nim` source extractor; a
    Nim native-traced run uses `shallowHashNative`. NO language-name special-
    casing anywhere (verified: the engine branches only on `TraceBackend`); this
    already worked through the M6 seams, confirmed not special-cased.

  Automated tests (`tests/t_nim_dual.nim`, all real — compile + edit + rebuild):
  - [X] test_name: nim_source_traced_uses_source_hash_and_decides
    description: over `trace_source/` + calc.nim: editing an EXECUTED proc
    (usedA) body ⇒ `idRerunChanged` naming it (sibling usedB not listed); editing
    an UNEXECUTED proc (unusedC) ⇒ `idSkipUnchanged`. (Source-text precision.)
  - [X] test_name: nim_native_traced_uses_instruction_hash_and_decides
    description: over `trace_native/` + the nim-compiled binary: edit + rebuild
    an executed proc (usedA) ⇒ `idRerunChanged`; edit + rebuild an unexecuted
    proc (unusedC) ⇒ `idSkipUnchanged`. The executed procs are pure
    position-independent leaves ordered BEFORE unusedC, so the unusedC edit does
    not change their instruction bytes nor their symbol names — guards assert the
    executed leaves' instruction-byte hashes AND mangled names are unchanged
    across the edit (and unusedC's own hash DID change), so the skip is genuine.
    (Instruction-byte precision, NOT source.)
  - [X] test_name: same_nim_program_different_backend_picks_different_strategy
    description: asserts `detectBackend(trace_source) == tbSourceInterpreted` and
    `detectBackend(trace_native) == tbNativeDwarf`, then drives the SAME
    comment-only edit to usedA through BOTH: the source-text hasher sees a change
    ⇒ `idRerunChanged`, while the native instruction-byte hasher sees
    byte-identical machine code (a comment is not emitted) ⇒ `idSkipUnchanged` —
    same program, same edit, OPPOSITE decision, proving the per-trace strategy is
    genuinely different and chosen by backend alone.

  Acceptance: Nim works on both paths with the strategy chosen by backend
  detection, no language-name branching. (All 4 M9 tests pass; full Phase-1 +
  M6/M7/M8 regression green, including M3 `t_multilang_extractors` after the
  `.nim` extractor addition.)

* M10 — Full language→strategy matrix
  :STATUS: complete

  Goal: make "all CodeTracer languages" concrete — a single dispatch table
  mapping every supported language to its `(TraceBackend, DependencyDiscovery,
  ShallowHash)` and demonstrate the representative groups end-to-end.

  Deliverables:
  - [X] A `languageStrategy(lang): LanguageStrategy` table covering the
    CodeTracer Language-Support-Matrix groups: interpreted (Python, Ruby, JS,
    TypeScript, Lua, WASM ⇒ source path), native (C, C++, Rust, Go, Pascal, D,
    Fortran, Ada, Crystal, Odin, V, plus Lean/Julia/Assembly ⇒ native path), Nim
    (dual). Each entry cites the matrix in `matrixCitation`. Implemented in
    `libs/repro_ct_incremental/src/repro_ct_incremental/language_matrix.nim`
    (re-exported from the package). Unknown/empty language ⇒ a clear `Err`
    (conservative re-run), never a wrong hash, never a silent skip.
    THE TABLE IS ADVISORY: it maps language→expected-strategy for reporting and
    validation (`expectedBackends`/`backendIsExpected`). The engine still detects
    the REAL backend from the trace via `detectBackend`, which remains the SINGLE
    SOURCE OF TRUTH for which strategy runs — documented on the module + on
    `languageStrategy` so there is no second source of truth that can drift. Nim
    is modelled as DUAL (BOTH backends valid); the per-trace backend wins.
  - [X] At least one passing end-to-end skip/rerun test per MECHANISM group
    (interpreted = Python via the M3 fixture; native = C via the M8 real compiled
    fixture; Nim dual via the M9 fixtures, BOTH arms) selected through the table
    — proving the SAME engine covers the whole matrix with only the strategy
    varying. Each group asserts the table's expected backend matches
    `detectBackend` (authoritative), then drives a correct RERUN (edit an
    executed function) AND a correct SKIP (edit an unexecuted function).
  - [X] Docs: this table (below) + a `libs/repro_ct_incremental/README.md`
    "Supported languages" section listing every supported language and its path,
    explicitly noting LIVE recordings vs HAND-CRAFTED fixtures (honest: in this
    prototype the TRACES are hand-crafted in the real schemas; the
    instruction-byte hashing + source extraction run on REAL artifacts — real
    source files and binaries the tests compile at test time).

  Automated tests (`tests/t_language_matrix.nim`):
  - [X] test_name: language_strategy_table_covers_matrix
    description: every language group in the matrix (the interpreted, native, and
    Nim-dual groups encoded in-test) resolves to the expected mechanism with the
    expected backend(s)/discovery/hash and a matrix citation; aliases resolve; an
    unknown/empty language ⇒ `Err`. Fails if a group becomes unclassified.
  - [X] test_name: representative_language_per_group_decides_end_to_end
    description: Python (interpreted) + C (native, real binary) + Nim (dual, BOTH
    arms) each drive a correct skip AND a correct rerun through the table → the
    engine, with the table's expected backend confirmed against `detectBackend`.
  - [X] test_name: full_phase2_suite_has_no_regressions
    description: with `--ct-incremental` absent the watch decision is the
    byte-for-byte legacy run path (the disabled `WatchCtIncrementalGate` via
    `gatedWatchDecision` short-circuits to `weaRun`/``ct-incremental-disabled``
    without consulting the engine; enabling it delegates to the real seam). As
    part of verification the ENTIRE lib suite (all 11 M0-M9 test files) +
    `repro_cli_support/tests/t_watch_ct_incremental_flags.nim` +
    `nim check libs/repro_cli_support/src/repro_cli_support.nim` (exit 0) all run
    green, and `nim check repro.nim` now compiles (exit 0) — the unrelated
    `Executable` error noted in M5/M6 was fixed upstream after the rebase, so the
    whole-project caveat no longer applies.

  Acceptance: a single table + engine covers every CodeTracer language group;
  representative end-to-end tests are green for interpreted, native, and Nim-dual.

** The language → strategy table (every language cites the matrix)

   Authoritative language list: `codetracer-specs/Language-Support-Matrix.md`
   ("Last updated: March 2026"). `detectBackend(traceDir)` is the AUTHORITATIVE
   per-trace source of truth; this table is ADVISORY (reporting/validation).

   | language    | mechanism          | expected backend(s)                    | discovery / shallow-hash        | matrix citation                                                |
   |-------------+--------------------+----------------------------------------+---------------------------------+----------------------------------------------------------------|
   | Python      | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | Platform Support Overview row 'Python' (Supported, Linux; interpreted recorder) |
   | Ruby        | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | Platform Support Overview row 'Ruby'                           |
   | JavaScript  | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | Platform Support Overview row 'JavaScript' (Planned; JS recorder) |
   | TypeScript  | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | traced via the JS recorder (TS runs as JS); classified with row 'JavaScript' |
   | Lua         | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | Platform Support Overview row 'Lua' (Planned; Lua recorder)    |
   | WASM        | source/interpreted | tbSourceInterpreted                    | canonical Function/Call · source-text | Wasm/wasmi recorders (managed runtime, not native machine code) |
   | C           | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'C' (LLDB + DWARF, rr)         |
   | C++         | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'C++'                          |
   | Rust        | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Rust'                         |
   | Go          | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Go' (Delve + DWARF)           |
   | Pascal      | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Pascal'                       |
   | D           | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'D'                            |
   | Fortran     | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Fortran' (GDB + DWARF)        |
   | Ada         | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Ada' (GDB + DWARF + GNAT)     |
   | Crystal     | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Crystal'                      |
   | Odin        | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Odin'                         |
   | V           | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'V' (DWARF via C backend)      |
   | Lean        | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Lean' (DWARF via C; partial values — still native) |
   | Julia       | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Recording Backends → Linux row 'Julia' (partial; native capture) |
   | Assembly    | native / MCR       | tbNativeDwarf                          | native calltrace · instruction-bytes | Platform Support Overview row 'Assembly' (Partial; native machine code) |
   | Nim         | DUAL               | tbSourceInterpreted AND tbNativeDwarf  | per-trace (detectBackend wins)  | Recording Backends → Linux row 'Nim' (DWARF via C backend) + a materialized-source path |

   Validation in this prototype: Python/Ruby/JS source extraction + C and Nim
   instruction-byte hashing run on REAL artifacts (real source files; binaries
   the tests compile with `cc`/`nim c` at test time). The TRACES (executed-set
   ingestion) are HAND-CRAFTED in the real schemas (no live recorder/MCR-RR in
   the dev shell). Languages marked "table-only" in the README are classified +
   covered by the matrix-coverage test but not separately exercised end-to-end
   (their MECHANISM is, via the representative-per-group tests). See
   `libs/repro_ct_incremental/README.md` for the full LIVE-vs-hand-crafted note.

* ============================================================================
* PHASE 3 — symBodyHash, modern CTFS, and LIVE-recording integration tests
* ============================================================================

  Three follow-ups requested after Phase 2:
  - **M11**: take advantage of Nim's compile-time *deep* hash (`symBodyHash`)
    when the unit-testing library reports it, while KEEPING the existing runtime
    shallow path as the fallback (used when the engine runs without that library).
  - **M12**: consume the **modern CTFS `.ct`** trace format (via
    `codetracer-trace-format-nim`), not the legacy 3-file JSON.
  - **M13**: replace hand-crafted trace fixtures with **live recordings from the
    real (native) recorders**, built per the codetracer-specs build-siblings
    strategy. The pure-Ruby recorder is the test-only oracle and is NOT used
    here; the native recorders emit CTFS.

* M11 — Nim symBodyHash optimisation (deep when reported; shallow retained)
  :STATUS: complete

  Spec: §16.2 (`symBodyHash` = compile-time deep hash over the transitive call
  graph), §16.3 (library embeds per-test `bodyHash` in `--list-json`), §16.5 /
  §3.7 (runner compares catalog `bodyHash`es between builds, skips unchanged).

  Goal: when the CodeTracer unit-testing library (`ct_test_unittest_parallel`)
  reports a per-test `symBodyHash` via the catalog, use that DEEP hash directly
  (skip iff unchanged, no trace / no shallow combination); otherwise FALL BACK to
  the existing runtime shallow path (M1/M9), which must NOT be removed.

  Catalog source taken — PATH (1b), with the reason CITED. The in-tree library
  `ct_test_unittest_parallel` does NOT (yet) emit a `bodyHash`/`symBodyHash`
  field in its `--list-json`: `emitListJson`
  (`libs/ct_test_unittest_parallel/src/ct_test_unittest_parallel.nim` ~line 157)
  writes only `name`/`suite`/`file`/`line`. So the §3.6 "Full" tier
  (`bodyHash` in `--list-json`) is unavailable out of the box and path (1a) is
  not achievable here. Instead M11 produces a GENUINE `symBodyHash` catalog
  directly via `std/macros.symBodyHash` (§16.2): a tiny macro + fixture
  (`libs/repro_ct_incremental/tests/symbodyhash_catalog.nim`'s `symBodyHashOf`)
  records `symBodyHash(theProc)` for each "test" proc into a flat
  `{testId: bodyHash}` catalog JSON. The catalog reader
  (`src/repro_ct_incremental/catalog.nim`) ALSO accepts the §16.3 `--list-json`
  `{tests:[{name,bodyHash}]}` shape so it is forward-compatible the moment the
  library starts emitting the field. Both the reader and the macro/fixture cite
  §16.2/§16.3.

  Deliverables:
  - [X] Catalog reader: `src/repro_ct_incremental/catalog.nim` parses a catalog
    into `BodyHashCatalog` (`test → bodyHash`), accepting BOTH the flat
    `{testId: bodyHash}` shape (the path-1b macro output) AND the §16.3
    `--list-json` `{tests:[{name,bodyHash}]}` shape. Empty/garbled ⇒ `Err`
    (⇒ fall back to shallow / re-run, never a silent skip); an empty-string hash
    is treated as ABSENT. The schema decision (no library bodyHash today ⇒ path
    1b) is documented + cited in the module header and in the M11 test header.
  - [X] `decideByCatalog*(testId; catalog; cache)` — catalog `bodyHash` equal to
    the cached `bodyHash` ⇒ `idSkipUnchanged`; differ ⇒ `idRerunChanged`; testId
    absent from cache ⇒ `idRunFresh`. NO trace, NO shallow hashing. Guards:
    non-deterministic ⇒ `idRerunNonDeterministic`; a cache entry with no recorded
    deep hash ⇒ conservative `idRerunChanged` (never a skip against an absent
    baseline). `recordBodyHash*` records a test purely by its deep hash (no
    trace).
  - [X] A TIERED selector `decideTiered*(testId; catalog; traceDir; sourceRoot;
    cache)`: DEEP path (`decideByCatalog`) iff the catalog reports a `bodyHash`
    for the test (traceDir/sourceRoot NOT consulted); else dispatch to the
    existing backend shallow `decide` (M6/M9). Documented
    "deep-when-the-library-reports-it, shallow-otherwise" on the proc, and that
    removing the shallow path would break the no-library case (every non-Nim
    language + Nim built without the library).
  - [X] `record` stores the catalog `bodyHash` alongside `{deepHash, deps}` (new
    optional `bodyHash` param; cache `version` bumped 2 → 3 per M5 ⇒ a v2 file
    re-runs). The two paths update/compare INDEPENDENTLY: a `record` WITH a
    catalog stores the bodyHash AND the shallow deps; a `record` without one
    stores the deps and leaves `bodyHash` empty.

  Automated tests (`libs/repro_ct_incremental/tests/t_symbodyhash.nim`, all real
  — genuine compile-time symBodyHashes + the real m0 Ruby fixture/trace):
  - [X] test_name: catalog_bodyhash_unchanged_skips_without_a_trace
    description: an unchanged genuine catalog hash ⇒ `idSkipUnchanged` via the
    DEEP path with a deliberately-BROKEN traceDir+sourceRoot that would fail-safe
    re-run if the shallow path were consulted — proving the deep path ran and the
    shallow path was never touched.
  - [X] test_name: catalog_bodyhash_changed_reruns (a changed genuine hash ⇒
    `idRerunChanged`).
  - [X] test_name: no_catalog_falls_back_to_shallow_path — with NO catalog
    bodyHash, the tiered selector decides EXACTLY as the existing `decide` (the
    same real m0 unchanged-skip AND executed-function-change-rerun cases run BOTH
    ways, asserted byte-identical incl. the changed-func set) — shallow path
    intact and used.
  - [X] test_name: deep_preferred_over_shallow_when_both_available — BOTH a
    catalog bodyHash AND a real trace/source exist; the source is UNCHANGED (so
    shallow alone would SKIP) but the catalog reports a CHANGED hash, and the
    tiered selector RERUNS — proving deep is chosen; a static over-estimate is a
    SAFE re-run, never a false-skip (the inverse same-hash case skips only on
    byte-equal deep hashes). A supporting guard asserts the fixture symBodyHashes
    are real (non-empty) and discriminating.

  Acceptance: deep `symBodyHash` decisions when reported; shallow retained + used
  otherwise; no false-skip on either. (All M11 tests green; full M0-M10 + M12 +
  M13 regression green — see the report; the shallow-path M1/M9 tests pass
  unchanged.)

* M12 — Consume modern CTFS `.ct` traces
  :STATUS: complete

  Goal: the engine reads the modern CTFS `.ct` bundle (what the native recorders
  emit), not the legacy 3-file JSON.

  Deliverables:
  - [X] CTFS is read via the `ct-print --json-events` SUBPROCESS (the documented
    prototype stand-in; linking `codetracer-trace-format-nim`'s reader directly
    is the production path, documented on `ctfs_trace.nim`). The subprocess
    avoids pulling the trace-format-nim build (libzstd flags + its module tree)
    into reprobuild's lib build for the prototype. `ct-print` resolution:
    `CT_PRINT` env → `PATH` → the known build path `/tmp/ctprint_build/ct-print`
    (the M12 test setup builds it there once and reuses it). `ct-print`
    unavailable ⇒ `Err` ⇒ the engine re-runs (never skips).
  - [X] A CTFS `DependencyDiscovery` impl —
    `ctfs_trace.readExecutedFunctionsCtfs(traceDirOrCtFile)`: runs `ct-print
    --json-events`, parses the MODERN `type`-tagged event array
    (`path`/`function`/`call`/`step`, NOT the legacy externally-tagged
    `{"Function":…}` form), maps `call.function_id → function.name` for the
    executed SET, and resolves each function's source `file`/`defLine`
    best-effort from the call's `entry_step` step. Parsing is TOLERANT of
    non-UTF-8 `value`/`data` bytes: the raw subprocess output is sanitized to
    valid UTF-8 (invalid bytes ⇒ U+FFFD) before `std/json`, so the CBOR value
    bytes inside `data` strings never break the structural records (which are
    pure ASCII). Accepts a `.ct` file OR a dir containing exactly one `.ct`.
  - [X] Backend wiring: a NEW `tbSourceCtfs` backend pairs CTFS discovery with
    the SAME source-text shallow hasher the `tbSourceInterpreted` path uses (an
    interpreted-language `.ct` ⇒ source-text identity). It is selected by an
    EXPLICIT `recorder_backend: "ctfs-interpreted"` metadata signal, so a bare
    `.ct` container with no explicit metadata STILL detects as `tbNativeDwarf`
    (instruction-byte hashing) — the existing native and source-JSON paths are
    byte-for-byte untouched (M6/M8 detection tests unchanged and green). The
    engine's readability probe + `currentDepLocations` were extended for the new
    backend (CTFS deps resolve under `sourceRoot` exactly like the source path).
  - [X] The engine decides over a REAL `.ct` bundle end-to-end (record/decide).

  Automated tests (`tests/t_ctfs_reader.nim`; fixture `tests/fixtures/m12_ctfs/`
  — a REAL `ruby.ct` recorded from `live_demo.rb` by the native Ruby recorder,
  committed alongside the `.rb` source + a README documenting the record + the
  `ct-print` build commands):
  - [X] test_name: reads_executed_functions_from_a_ct_bundle
    description: over the real `.ct`, the reader returns exactly
    {`<top-level>`,`main`,`used_a`,`used_b`} with `file`/`defLine` resolved from
    the entry steps; `unused_c` (defined-but-never-called) is absent. A companion
    `reads_executed_functions_from_a_trace_dir_containing_a_ct` proves the
    dir-containing-a-`.ct` resolution.
  - [X] test_name: engine_decides_over_ctfs
    description: record/decide over the `.ct` + its `.rb` source — identical
    source ⇒ `idSkipUnchanged`; editing an EXECUTED function (`used_a`) ⇒
    `idRerunChanged` naming it (and NOT `used_b`); editing the UNEXECUTED
    `unused_c` ⇒ `idSkipUnchanged` (function-level precision over one shared
    source file).
  - [X] test_name: ctfs_read_error_falls_back_to_rerun
    description: three failure modes — a corrupt `.ct` ⇒ `record` Errs (never
    records a skip-eligible entry); a missing bundle at decide time ⇒
    `idRerunFailSafe` (never a skip); `ct-print` unavailable (bogus `CT_PRINT`)
    ⇒ the reader Errs ⇒ the engine re-runs.

  Acceptance: the engine consumes CTFS `.ct` for real; legacy JSON remains for
  the oracle/legacy fixtures but CTFS is the live path. A CTFS read error can
  NEVER yield a skip (the only skip path is `idSkipUnchanged`, reached only when
  the bundle reads, `ct-print` resolves, the backend detects `tbSourceCtfs`, and
  every executed function's current source hash equals its recorded hash).

* M13 — Live-recording integration tests (native recorders, all languages)
  :STATUS: complete

  Goal: real integration tests — build the native recorders, record real
  programs, run the engine over the resulting CTFS, per language group. No
  hand-crafted traces where a recorder is available.

  Deliverables:
  - [X] A harness that builds a recorder in ITS dev shell per the build-siblings
    strategy (`direnv exec <recorder-repo> <build>`) and records a small program
    into a `.ct` bundle, then drives the engine. Build once, reuse.
    Implemented as `libs/repro_ct_incremental/tests/live_record.nim`:
    `runInRecorderShell(repo, cmd)` enters the sibling's Nix dev shell via
    `direnv exec` (cd-ing into the repo, since `direnv exec` resets cwd) and runs
    the command there. Each recorder has a cheap on-disk BUILT-MARKER predicate
    (`rubyRecorderBuilt`/`pythonRecorderBuilt`/`jsRecorderBuilt`/
    `nativeRecorderBuilt`); `ensure*Built` runs the heavy build ONLY when the
    marker is absent, so a build happens at most once per host and re-runs reuse
    it. `ensureCtPrintBuilt` reuses the M12 known-build-path cache
    (`/tmp/ctprint_build/ct-print`). Interpreted recorders stamp the recorded
    trace dir with `recorder_backend: "ctfs-interpreted"` (`markCtfsInterpreted`)
    so `detectBackend` routes the live `.ct` through `tbSourceCtfs` (source-text
    hashing); the native recorder leaves a bare `.ct` (⇒ `tbNativeDwarf`).
  - [X] LIVE recordings via the PRODUCTION recorders (NOT the pure-Ruby oracle).
    RESULTS ON THIS HOST (arm64 macOS), all REAL attempts:
      * Ruby — ✅ LIVE end-to-end. `just build` (native Rust extension) then
        `codetracer-ruby-recorder --out-dir <dir> prog.rb` produces a real
        `.ct`; the engine decides over it. Executed set from the LIVE bundle is
        {`<top-level>`,`main`,`used_a`,`used_b`}; `unused_c` (never called) is
        absent.
      * Python — ✅ LIVE end-to-end. `just venv 3.13 dev` (build once; ~1-2 min
        maturin compile, cached via the on-disk native extension built-marker)
        then `.venv/bin/python -m codetracer_python_recorder --out-dir <dir>
        prog.py` produces a real `.ct`; the engine decides over it. Executed set
        from the LIVE bundle is {`<__main__>`,`main`,`used_a`,`used_b`};
        `unused_c` (never called) is absent. NOTE: the prior M13 revision wrongly
        gated Python as "the dev shell won't build" — that used the WRONG entry
        (`just dev` + `uv`). After the recorder's FLAKE FIX gating
        `cargo-llvm-cov` off darwin (pushed upstream to the recorder repo),
        `just venv 3.13 dev` builds and records LIVE here, as the test proves.
      * JavaScript — ✅ LIVE. `just build` compiles the napi addon
        (`recorder_native`) and the recorder records a real CTFS bundle; the
        engine extracts exactly the executed functions and decides skip/rerun.
        An earlier revision WRONGLY gated this as an upstream break
        (`NimTraceWriter` missing `enable_column_breakpoints_support` /
        `enable_column_motions_support`): those methods DO exist on the
        column-aware-tracing API — the failure was STALE sibling checkouts
        (`codetracer-trace-format` + `codetracer-trace-format-nim` behind their
        mainline, so the addon built against an old writer API and SIGSEGV'd at
        record time). With the siblings synced the recorder builds and records
        cleanly; the test ASSERTS success (no accepted gate). The recorder
        nests its bundle under `<out-dir>/trace-N/`, so the harness locates the
        `.ct` recursively and uses that subdir as the trace dir.
      * Native / Nim via MCR/RR — ⛔ GATED (LOUD, asserted) on a verified
        platform/format limitation — NOT an upstream build break. An earlier
        revision wrongly claimed "recorder CI red on main+dev / licensing FFI
        dylib cannot load"; that was incorrect. With the recorder synced to
        mainline `dev`, `ct-mcr` (from `ct_cli`) BUILDS CLEAN on arm64-macOS
        (incl. the macOS dylib `@rpath`/codesign steps) and RECORDS a real `.ct`
        (exit 0, ~200 low-level events). The gate is downstream: on this host the
        native `.ct` decodes (via `ct-print --json-events`) to only a `path`
        record — no `Function`/`Call` events — so the engine has no executed-
        function set; and the engine's native backend still reads a legacy
        `native_calltrace.json` sidecar the modern CTFS recorder no longer writes.
        Full function-level native traces come from the Linux MCR/RR path; wiring
        the native backend to consume the modern CTFS `.ct` and validating it on
        a Linux MCR host is the documented follow-up. The gate asserts a non-empty
        diagnostic with the VERIFIED reason — never a `unittest.skip`.
  - [X] The interpreted group is validated with GENUINE live CTFS recordings
    (Ruby, Python AND JavaScript) consumed by the engine. JS is live once the
    workspace siblings (`codetracer-trace-format` + `-trace-format-nim`) are
    synced to mainline — the earlier "JS upstream break" was a stale-sibling
    misdiagnosis, now corrected. (Native was subsequently brought LIVE on
    arm64-macOS via compile-time instrumentation in M14/M15; the hand-crafted
    Phase-1/2 native fixtures remain only as schema anchors, and the OS-level
    Intel PT / MCR path stays gated to a Linux PT/MCR host — see M16.) The
    README's M13 section marks exactly which languages are LIVE vs gated, with
    the real commands and verified root causes.

  Automated tests:
  - [X] test_name: live_ruby_recording_decides_end_to_end
    (`tests/t_live_ruby.nim`) — writes a real 3-function `.rb`, records it LIVE,
    asserts the live executed set, then a correct SKIP (unchanged), a correct
    RERUN (edit executed `used_a` ⇒ `idRerunChanged` naming it, not `used_b`),
    and a correct SKIP (edit unexecuted `unused_c`). PASSES on this host.
  - [X] test_name: live_python_recording_decides_end_to_end
    (`tests/t_live_python.nim`) — writes a real 3-function `.py`, records it LIVE
    with the production maturin recorder, asserts the live executed set
    ({`<__main__>`,`main`,`used_a`,`used_b`}; `unused_c` absent), then a correct
    SKIP (unchanged), a correct RERUN (edit executed `used_a` ⇒ `idRerunChanged`
    naming it, not `used_b`), and a correct SKIP (edit unexecuted `unused_c`).
    PASSES on this host. (Mirrors/edits the RECORDED source path from the trace,
    not the local write path, so the macOS `/var/folders` ↔ `/private/var`
    canonicalisation cannot misplace the mirror.)
  - [X] test_name: live_js_recording_decides_end_to_end
    (`tests/t_live_js.nim`) — writes a real 4-function `.js`, records it LIVE
    with the production SWC + napi recorder, asserts the live executed set
    ({`<module>`,`main`,`used_a`,`used_b`}; `unused_c` absent), then a correct
    SKIP (unchanged), a correct RERUN (edit executed `used_a` ⇒ `idRerunChanged`
    naming it), and a correct SKIP (edit unexecuted `unused_c`). ASSERTS success
    (no accepted gate); fails loudly with the captured diagnostic if the
    recorder cannot build/record (e.g. stale sibling checkouts). PASSES here.
  - [X] test_name: native_recording_runs_where_supported_else_gated
    (`tests/t_live_native.nim`) — compiles a real C binary and records it LIVE
    with `ct-mcr` (builds clean from the latest dev; records a real `.ct`, exit
    0). On this host the engine cannot extract an executed-function set from the
    native `.ct` (it decodes to only a `path` record — no Function/Call events —
    and the engine's native backend still expects a legacy `native_calltrace.json`
    sidecar), so the test emits the LOUD asserted gate citing that VERIFIED
    platform/format limitation (NOT a build break). On a Linux MCR host that
    yields a readable native trace it drives the engine over the live `.ct`.
  - [X] test_name: full_suite_green_with_live_recordings
    (`tests/t_live_full_suite.nim`) — compiles and runs EVERY campaign test file
    (all 16 other lib test files, M0-M13) as subprocesses and asserts each exits
    0. Verified green (the 4 live tests + all pre-M13 engine tests).

  Acceptance: at least the interpreted group validated with genuine live CTFS
  recordings consumed by the engine; native/Nim either demonstrated or honestly
  gated with a loud diagnostic. (CORRECTED status, verified later in Phase 4:
  Ruby, Python AND JavaScript are all genuinely LIVE end-to-end here — the
  earlier "JS upstream break / `enable_column_*_support` API mismatch" was a
  STALE-SIBLING misdiagnosis, fixed by syncing `codetracer-trace-format` +
  `-nim` to mainline. Native is also LIVE here via compile-time instrumentation
  (M14/M15, the `ct_instrument` call-trace facet); the only remaining native
  gate is the OS-level Intel PT / MCR path, which is recorder-independent and
  needs a Linux PT/MCR host — see M16. Each gate prints the EXACT observed
  reason, never a misdiagnosed "dev shell won't build".)

# =============================================================================
# Phase 4 — Spec-faithful native discovery, dedicated call stream, ct integration
# =============================================================================
#
# Context (verified against the code, 2026-06):
#   * Materialized `.ct` writer (codetracer-trace-format/codetracer_trace_writer/
#     src/ctfs_writer.rs) emits a SINGLE combined `events.log` stream (+ `events.fmt`
#     format marker, `meta.dat` v3 with a feature-flag bitfield). The `calls.dat`/
#     `steps.dat` split from trace-events.md is NOT emitted. ct-print
#     (codetracer-trace-format-nim) already carries DORMANT V4 `call_stream` /
#     `step_encoding` readers — the split was started reader-side, not writer-side.
#   * Native call data: on Linux, `ct-mcr` produces a real call tree via the
#     EMULATOR (ct_emulator/calltrace.nim → CallNode{functionName,sourceFile,...}),
#     resolved through DWARF. Intel PT decoding exists in the RR fork
#     (ProcessorTraceDecoder) but is used ONLY for divergence detection, gated
#     behind the INTEL_PT_DECODING build flag, x86/Linux-only. On arm64-macOS the
#     MCR has no emulator, so the `.ct` carries no call data (why ct-print showed
#     only a `path` record). Compile-time instrumentation (the spec's alternative,
#     MCR-Calltrace-Design §22d shadow stack) is SPECIFIED but UNIMPLEMENTED — and
#     is the only native call-trace source that works on arm64-macOS.
#   * The prototype's native path (native_trace.nim) reads a hand-crafted
#     `native_calltrace.json` projection of the MCR CallTree; native shallow hash
#     (native_hash.nim, M7) hashes compiled instruction bytes (spec-correct).
#   * `ct test` (codetracer/src/ct) is a stub: a reprobuild-HCR e2e wrapper only.
#     No test discovery, baseline recording, executed-function extraction, or
#     incremental selection (the whole §16.7 feature) exists there yet.
#
# Sequencing rationale: M14-M15 (compile-time instrumentation) are fully testable
# on arm64-macOS and close the native end-to-end gap here. M16 (PT/MCR) is the
# Linux-gated spec-primary path — unit-tested, live-gated honestly. M17 (calls.dat)
# is the first compat-preserving step of the trace-events.md "Event Stream Redesign".
# M18 ports the engine into `ct test`. M19 ships docs.

* M14 — Native call trace via compile-time instrumentation (cross-platform)
  :STATUS: complete

  REWORK NOTE (2026-06-21, RESOLVED): the first M14 cut added a NEW
  `-finstrument-functions` C runtime (`csrc/ct_instrument_runtime.c`) + build
  logic inside reprobuild. That DUPLICATED the existing compile-time
  instrumentation plugin `codetracer-native-recorder/ct_instrument`. The rework
  REMOVED that duplicate (`csrc/ct_instrument_runtime.c` is deleted) and added a
  cleanly-separated, independently-selectable CALL-TRACE FACET to ct_instrument
  instead:
    * `ct_instrument/src/ct_instrument/calltrace/ct_calltrace_sink.c` + `.h` —
      the `-finstrument-functions` executed-function sink (resolves names via
      `dladdr(...).dli_sname`, de-dups, appends each new name once to the file
      named by `CT_CALLTRACE_OUT`). Hooks/helpers are `no_instrument_function`;
      thread-safe; allocation-light; inert when the env var is unset.
    * `ct_instrument/src/ct_instrument/ct_calltrace.nim` — `buildWithCalltrace`
      (mirrors `ct_gcc_wrapper.buildWithShadowStack`: compile the sink without
      instrumentation, compile the target with `-finstrument-functions`, link;
      `-ldl` on Linux, none on macOS) + `runWithCalltrace`. Exported from
      `ct_instrument.nim`. Test: `ct_instrument/tests/test_calltrace_sink.nim`.
  This facet is DISTINCT from the shadow stack (which keeps a LIVE per-thread
  call stack for the Call-Trace Pane) — it persists an executed-function SET, a
  different problem — so the shadow-stack / tick / pad/atomic/rcb facets are all
  UNCHANGED. reprobuild now CONSUMES the facet through the build-siblings
  strategy (`direnv exec codetracer-native-recorder ...`); it carries no
  instrumentation runtime of its own.

  Goal: implement the spec's "compile-time instrumentation that produces the call
  trace" alternative (Nim-Parallel-Test-Framework §16.7; MCR-Calltrace-Design §22d
  shadow stack) for native targets, so native runtime dependency discovery works
  WITHOUT Intel PT / RR / the MCR emulator — and is testable on arm64-macOS.

  Deliverables:
  - [X] A call-trace facet on the EXISTING `ct_instrument` plugin using the
    `-finstrument-functions` ABI (`__cyg_profile_func_enter` /
    `__cyg_profile_func_exit`) that records the set of entered functions into the
    file named by `CT_CALLTRACE_OUT`. Thread-safe, bounded, no heap churn on the
    steady-state hot path; inert when the env var is unset; the file format is
    documented. Implemented in
    `codetracer-native-recorder/ct_instrument/src/ct_instrument/calltrace/ct_calltrace_sink.c`
    (+ `.h`). Name resolution is via `dladdr(this_fn,&info).dli_sname` (robust to
    PIE/ASLR) rather than mapping raw runtime PCs back to static symbol
    addresses — so the sink records NAMES directly, one per line. The facet is
    SEPARATE from and does not entangle the existing shadow-stack runtime.
  - [X] A Nim build/run helper that compiles+links a native test fixture with
    `-finstrument-functions` against the sink, runs it with `CT_CALLTRACE_OUT`
    set, and produces the name log — `ct_instrument/ct_calltrace.nim`'s
    `buildWithCalltrace` (mirrors `ct_gcc_wrapper.buildWithShadowStack`) +
    `runWithCalltrace`, exported from `ct_instrument.nim`. `dladdr` needs `-ldl`
    on Linux, none on macOS; handled per-host. reprobuild's `native_instrument.nim`
    (`compileInstrumented` / `runInstrumented` / `instrumentAndRun`) CONSUMES the
    facet through the build-siblings strategy (`direnv exec
    codetracer-native-recorder ...`), preserving its public proc names/signatures
    so the engine + tests are unchanged.
  - [X] Function-name → executed-set resolution yielding the de-duplicated
    `seq[ExecutedFunction]` (`readExecutedFunctionsInstrumented`); best-effort
    pruning of names not in the binary's own symbol table reuses
    native_hash.nim's `nativeFunctionTable` (Mach-O + ELF). Leading Mach-O
    underscore stripped to match the native_hash symbol keys.
  - [X] Fail-safe: any compile/run/read error, a missing/empty log, or an
    unresolvable capture ⇒ `Err` (engine re-runs, never a false skip).

  Automated tests:
  - [X] ct_instrument facet: a real 4-function C program built with the
    call-trace facet, run, asserts the executed set == {used_a, used_b, main}
    with `unused_c` ABSENT, plus de-dup and inert-when-unset cases —
    `codetracer-native-recorder/ct_instrument/tests/test_calltrace_sink.nim`
    (passes in the native-recorder dev shell on arm64-macOS).
  - [X] reprobuild side: the same 4-function executed-set assertion sourced from
    the facet, on arm64-macOS (no skip, no gate). `tests/t_native_instrument.nim`.
  - [X] Missing-log, empty-log, unresolvable-capture, missing-source, and
    non-zero-exit cases all return `Err`.

  Acceptance: the native executed-function set is derived from a GENUINE
  compile-time-instrumentation run (via the existing ct_instrument plugin) on
  this host, deterministically.

* M15 — Engine consumes the real native call trace (retire native_calltrace.json)
  :STATUS: complete

  REWORK NOTE (2026-06-21, RESOLVED): the engine wiring (backends.nim routing,
  native-instrumented detection, the clean-binary instruction-byte hash, and the
  live t_live_native skip/rerun/skip-unexecuted decision) was correct and is
  KEPT unchanged. The only change from the M14 rework: the harness now obtains
  the executed-function set from the existing `ct_instrument` plugin's call-trace
  facet (`buildWithCalltrace` / `CT_CALLTRACE_OUT`, via `direnv exec
  codetracer-native-recorder ...`) rather than reprobuild's duplicated runtime —
  `native_instrument.nim` was rewritten to drive that facet while preserving its
  public procs. `recordNativeInstrumentedLive` is unchanged at the call sites; it
  reuses the rewritten `instrumentAndRun`. The end-to-end macOS-live native
  decision (`t_live_native.nim`) and the full live suite
  (`t_live_full_suite.nim`) both pass green on this arm64-macOS host.

  Goal: wire M14's instrumentation-produced call trace into the engine's native
  backend so the native incremental decision (skip / re-run) runs end-to-end on
  arm64-macOS. Keep the M7 instruction-byte shallow hash. The hand-crafted
  `native_calltrace.json` reader is retired in favour of the real artifact (kept
  only as an explicitly-labelled legacy fixture path if still needed by old tests).

  Deliverables:
  - [X] `native_trace.nim` reads the instrumentation call trace as a FIRST-CLASS
    native dependency-discovery source. A flavour dispatch
    (`readExecutedFunctionsNativeAny` / `nativeTraceBinaryAny` /
    `nativeTraceDirReadableAny`) selects, per trace dir, the GENUINE
    instrumentation capture (M14: `native_instrument_calls.log` +
    `instrumented_prog`) — preferred when present — else the legacy
    `native_calltrace.json` projection (M8, kept as an explicitly-labelled legacy
    path so every M8 test keeps passing, with backward-compatible error messages).
    `detectBackend` routes an instrumentation-native trace dir to `tbNativeDwarf`
    (its `trace_db_metadata.json` marker is a native structural signal; the
    harness also stamps `recorder_backend: "native-instrumented"`, newly mapped to
    `tbNativeDwarf` in `backends.nim`). The native shallow hash stays the M7
    instruction-byte hasher. CRUCIAL: the hash reads a CLEAN (non-instrumented)
    `recorded_prog` build of the same source, NOT the instrumented binary —
    `-finstrument-functions` makes every function's bytes relocation-sensitive to
    unrelated edits, so hashing the instrumented binary would re-hash the executed
    set on an `unused_c` edit (a safe but useless re-run). Instrumentation is the
    DISCOVERY tool; the hash reflects the production binary
    (`native_trace.instrumentHashBinaryPath`). The fail-safe invariant holds: any
    read/resolve error ⇒ re-run, never a false skip.
  - [X] `tests/t_live_native.nim` flips from the platform gate to a GENUINE live
    native decision on macOS: record via `recordNativeInstrumentedLive` (a new
    harness driver in `tests/live_record.nim` reusing M14's `native_instrument`),
    assert the executed set == {used_a, used_b, main} (unused_c ABSENT), then a
    correct SKIP (recompile unchanged), a correct RE-RUN (edit the executed used_a,
    recompile ⇒ idRerunChanged naming used_a, NOT used_b), and a correct SKIP
    (edit the unexecuted unused_c, recompile ⇒ idSkipUnchanged) — mirroring the
    live Ruby/Python/JS tests. NO gate, NO unittest.skip, asserts success on this
    arm64-macOS host. (The ct-mcr/MCR path is M16's concern and is not under test
    here.)

  Automated tests:
  - [X] End-to-end native skip/re-run over a real C binary, on arm64-macOS
    (`t_live_native.nim`).
  - [X] Full live suite stays green (`t_live_full_suite.nim`).

  Acceptance: native is LIVE end-to-end on this host via instrumentation — the
  native row in the README/matrix moves from gated to live.

* M16 — Intel PT native discovery (OS-level, recorder-independent; honest gate)
  :STATUS: deferred — requires an x86 + Intel-PT Linux host

  DEFERRAL NOTE (2026-06-21, owner-approved): Intel PT requires an x86 CPU with
  Processor Trace, Linux `perf_event` (or RR's PT capture), and `libipt` to
  decode — none available on the arm64-macOS dev host, and there is no real
  raw-PT trace to develop or validate against here. Per the campaign's standard
  (genuine live validation, no weak/unvalidated tests, reuse not duplicate),
  M16 is deferred to an x86+PT Linux host. Implementation guidance when picked
  up there: reuse the EXISTING RR `ProcessorTraceDecoder` (codetracer-rr,
  behind the `INTEL_PT_DECODING` build flag) rather than a fresh libipt binding;
  capture a real PT trace via `perf record -e intel_pt//` (recorder-independent —
  no MCR/RR recording required, per Intel-PT-Call-Trace-Research.md); decode →
  classify CALL/RET → reconstruct the call tree → resolve PCs to functions via
  DWARF → executed-function set; commit a real captured raw-PT fixture so the
  decode logic is unit-tested everywhere. On arm64-macOS the live native path
  stays M14/M15 instrumentation. (Deliverables below are unchanged.)

  Goal: derive the executed-function set from an **Intel PT control-flow trace**,
  decoded into call/return events. CORRECTION (2026-06-21): Intel PT is NOT tied
  to RR or MCR. Per Intel-PT-Call-Trace-Research.md, raw PT packets come from
  **`perf_event` OR an RR recording** and are decoded by **libipt** (Rust bindings
  `libipt-rs`) independently — "no perf dependency", "no MCR infrastructure
  required". Recording is ORTHOGONAL to producing a trace: PT can be captured on
  any OS/host with PT hardware, with or without an MCR/RR recording. So this
  milestone is an OS-level PT-decode path, not an RR-bound one.

  Deliverables:
  - [ ] Capture an Intel PT control-flow trace for a native test run via the
    OS facility (`perf record -e intel_pt//`), independent of RR/MCR. (An RR
    recording's PT data is an alternative source, not a requirement.)
  - [ ] Decode the PT stream with libipt's instruction-flow / block decoder
    (`libipt-rs`): classify CALL/RET (`pt_insn.iclass`) → reconstruct the call
    tree → resolve PCs to functions via DWARF → executed-function set
    (`seq[ExecutedFunction]`). Per the spec, decode once at import and build a
    persistent index.
  - [ ] (Optional, complementary) consume the Linux `ct-mcr` emulator CallTree
    (ct_emulator/calltrace.nim → CallNode) where an MCR recording exists — but the
    PT path does not depend on it.
  - [ ] Honest gate on hosts without PT hardware (e.g. arm64-macOS): the test
    prints the VERIFIED reason and asserts a documented gate — never a skip. On
    arm64-macOS the live native path is M14/M15 instrumentation; PT live
    assertions run on an x86 host with PT.

  Automated tests:
  - [ ] Unit tests for the PT-decode → call-tree → function-resolution logic over
    committed raw-PT fixtures (run everywhere, no PT hardware needed to decode a
    captured fixture).
  - [ ] Live end-to-end native decision asserted on an x86+PT host; honest gate
    elsewhere.

  Acceptance: the executed set is derived from an OS-level Intel PT decode
  (recorder-independent), unit-tested everywhere, live path honestly gated.

* M17 — Dedicated `calls.dat` call stream in materialized `.ct` (additive, compat)
  :STATUS: M17a complete (writer + ct-print + engine flag-detection + round-trip/engine tests); M17b db-backend SEEKABLE reader DONE (calls.dat served on-demand, call-tree routed off the full-load path, in_memory_trace_reader bounded+documented); engine calls.dat-preferring read path still deferred

  NOTE (2026-06-21): M17 is split into M17a (the dedicated `calls.dat` call
  stream in materialized `.ct` traces plus the format-level readers, and engine
  DETECTION of the stream — DONE) and M17b (the db-backend SEEKABLE reader for
  `calls.dat`, the engine actually READING `calls.dat` to avoid scanning the
  unified stream, and the `in_memory_trace_reader.rs` spec-violation fix —
  PENDING). The boxes below are checked per the M17a deliverables; the
  db-backend reader, the engine's calls.dat-preferring read path, and
  `in_memory_trace_reader` are left UNCHECKED/partial for M17b.

  Goal: implement the first, backward-compatible step of trace-events.md's "Event
  Stream Redesign" — emit a DEDICATED `calls.dat` call stream alongside the existing
  combined `events.log`, gated by a new `meta.dat` capability flag, so the call
  trace can be read without scanning the step/value events; activate ct-print's
  dormant `call_stream` reader; have the engine prefer `calls.dat` when present;
  AND wire the db-backend's SEEKABLE reader to consume it. (The full multi-stream
  migration — `steps.dat`/value/`events.dat`/varint interning — is a larger
  trace-format initiative beyond this campaign; this milestone lands ONLY the
  call-stream split, additively, breaking nothing.)

  IMPORTANT reader-side requirement (CORRECTION 2026-06-21): the db-backend
  (`codetracer/src/db-backend/`) is the production reader for materialized `.ct`
  traces and its architecture is SEEKABLE / not-fully-in-memory by design — to
  support a `.ct` loaded over the network without materializing the whole trace
  (Trace-Files-Overview.md §"Random-access seeking": CTFS block-level seeking
  O(log n) + Seekable-Zstd O(1) frame lookup + a supplementary event offset index;
  on-demand decompression with an LRU; multiple concurrent readers during
  recording). The earlier plan WRONGLY assumed the db-backend "consumes Vec<Event>
  and is format-agnostic" — that is the fully-in-memory path
  (`in_memory_trace_reader.rs`), NOT the seekable `ctfs_trace_reader`. A dedicated
  `calls.dat` stream MUST be exposed through the seekable reader so the call tree
  loads independently and on-demand (per trace-events.md "Call tree loads
  independently"), never by reading the whole trace into memory.

  Deliverables:
  - [X] Writer (codetracer-trace-format/codetracer_trace_writer): emit `calls.dat`
    call records per trace-events.md §"Call Stream Records", with its companion
    chunk index (`calls.idx`) for seekable access, in addition to `events.log`,
    behind a new `meta.dat` flag bit (e.g. has_call_stream). Old readers ignore
    the extra files; the combined stream is unchanged.
    (M17a: `CtfsTraceWriter::with_call_stream(true)` builds the call records
    from the same Call/Return/Step events that feed `events.log` via the new
    `call_stream::CallStreamBuilder`, encodes them per the spec wire format
    (byte-compatible with `call_stream.nim`), and writes `calls.dat` (chunked
    Zstd) + `calls.idx` (companion offset index per seekable-zstd.md) + a
    `meta.dat` carrying the new `FLAG_HAS_CALL_STREAM` bit. The flag bit is also
    added to the canonical Nim writer (`meta_dat.nim` bit 8 `FlagHasCallStream`),
    and the Nim `multi_stream_writer` — which always emits a v4 `calls.dat` —
    now stamps it.)
  - [X] db-backend SEEKABLE reader (`codetracer/src/db-backend/src/ctfs_trace_reader`):
    read `calls.dat` via CTFS block seeking + the Seekable-Zstd seek table + the
    companion index — on-demand, NOT fully into memory — so a network-loaded `.ct`
    materializes only the call records a request needs. Honor the
    not-fully-in-memory + concurrent-reader properties from Trace-Files-Overview.md.
    (M17b: new `ctfs_trace_reader::call_stream_source::SeekableCallStream` REUSES
    the M17a `codetracer_trace_reader::call_stream_reader::CallStreamReader`
    (it does NOT reimplement the format) to fetch a call by `call_key`,
    decompressing ONLY that call's Zstd chunk. `CTFSTraceReader::open` attaches it
    when `meta.dat` carries the `has_call_stream` flag (bit 8 — also added to the
    db-backend `meta_dat.rs` KNOWN_FLAGS_MASK so a split bundle's meta.dat is
    accepted, not rejected as "newer writer"). The db-backend `meta.dat` parser
    previously REJECTED bit 8 as unknown, so without this fix a split `.ct` failed
    to open at all. A `SeekableCallStream::chunk_decompressions()` counter makes
    the bounded-decompression property observable; the test asserts fetching one
    call inflates exactly one chunk over a 3-chunk stream. Concurrent readers each
    open their own `SeekableCallStream` over the read-only container (a per-reader
    `Mutex` guards the one-chunk cache), proven by an 8-thread test.
    LIMITATION (RESOLVED in M20): the M17a `CallStreamReader` requires the
    Rust-writer's `calls.idx` companion; the pre-M20 Nim `multi_stream_writer`'s
    `calls.dat` shipped no `calls.idx`. M20 fixes the Nim writer to emit a
    byte-compatible `calls.idx`, so a Nim-written split bundle is now SEEKABLE and
    `real_split_ct_serves_calls_seekably` no longer skips.)
  - [~] Address the `in_memory_trace_reader.rs` SPEC VIOLATION (CORRECTION
    2026-06-21): `InMemoryTraceReader::new(db: Db)` serves lookups over a
    FULLY-MATERIALIZED in-memory `Db` (HashMaps/Vecs of steps/values/cells from the
    whole trace) — it does not seek. The spec mandates CTFS files are NOT fully
    processed on load (Trace-Files-Overview.md §"Random-access seeking"), and the
    code already knows this: `trace_processor.rs` says traces "must be read through
    `ctfs_trace_reader::CTFSTraceReader::open`" and `dap_server.rs` calls
    `InMemoryTraceReader` "a safe placeholder". The `calls.dat`/`steps.dat` split
    exists precisely to enable on-demand loading. So: route the production read
    paths (DAP handlers, `diff.rs`, the `dap_server` placeholder) through the
    seekable `CTFSTraceReader`, and retire or strictly bound the full-load
    `in_memory_trace_reader` `Db` materialization (keep it, if at all, only as an
    explicitly-labelled small-trace/test fallback — never the network/large-trace
    path). The call tree must "load independently" via `calls.dat`, never by
    materializing the whole event stream into a `Db`.
    (M17b — DONE in part, HONESTLY bounded `[~]`:
     (1) The CALL TREE is now routed off the full-load path: `Calltrace::new`
         prefers the seekable `calls.dat` stream when the reader exposes one (new
         default-`None` `TraceReader::seekable_call_count`/`seekable_call` hooks,
         overridden by `CTFSTraceReader`); a `has_call_stream` `.ct` serves its
         call tree on-demand from `calls.dat`, NOT from a fully-materialized `Db`.
         Legacy readers return `None` and keep the exact prior behaviour.
     (2) `InMemoryTraceReader` is now explicitly DOCUMENTED + SCOPED (doc comment)
         as a small-trace / test / placeholder adapter ONLY — never the
         network/large-trace path; the production materialized read path is
         `CTFSTraceReader::open`, exactly as `trace_processor.rs` already
         mandates. The `dap_server.rs` MCR/emulator `InMemoryTraceReader` is an
         EMPTY `Db` placeholder (the emulator owns its state machine and never
         reads the `Db`), so it was already not a full-materialization of a large
         trace; this is now documented at the construction site too.
     WHAT REMAINS on the full-load path (and why, honestly):
       - The `TraceReader` trait's `call`/`step`/`variables_at`/... return
         BORROWED `&Db…` references, so `CTFSTraceReader` still materializes a
         `Db` for the non-call-tree lookups (steps, values, cells). A truly lazy
         seekable `steps.dat`/value path is the larger multi-stream migration the
         M17 goal explicitly scopes OUT ("beyond this campaign"). M17b lands the
         CALL-TREE seek (the one stream M17 actually split) and leaves the rest
         materialized — SAFE and non-regressing.
       - `diff.rs::index_diff` (a batch CLI subcommand, NOT the live DAP/network
         path) still calls `load_and_postprocess_trace` → `InMemoryTraceReader`.
         Re-routing it through the seekable reader is a larger refactor of
         `MaterializedReplaySession`/`FlowPreloader` and was left for a later
         pass to avoid regressing the diff-indexing path; it is not the
         network/large-trace GUI read path.)
  - [X] ct-print (codetracer-trace-format-nim): when the flag is set, read calls
    from `calls.dat` via the existing dormant `call_stream` reader; otherwise the
    legacy path. `--json-events` output is unchanged for consumers.
    (M17a: ct-print's `NewTraceReader` already sources call records from the v4
    `calls.dat` via the `call_stream` reader; the new `has_call_stream` flag is
    surfaced under `metadata.flags` in `--full`/`--meta-json` only.
    `--json-events` is proven byte-for-byte unchanged — a baseline ct-print and
    the new ct-print produce identical `--json-events` over the same bundle.)
  - [~] Engine (reprobuild ctfs_trace.nim): DETECT the dedicated `calls.dat`
    stream and prove results are identical with or without it; identical results
    either way.
    (M17a: new `ctfsHasCallStream` probes the `has_call_stream` flag via
    `ct-print --meta-json` (metadata-only, does not decode the event streams),
    and the engine proves the executed set is identical for a split vs a legacy
    bundle. Any subprocess/parse error ⇒ Err ⇒ re-run, never a silent skip.
    NOTE: `readExecutedFunctionsCtfs` still extracts the executed set from the
    unified stream via `ct-print --json-events` for BOTH bundles — it does NOT
    yet read `calls.dat` directly. Actually PREFERRING `calls.dat` (so discovery
    avoids scanning the larger step/value stream) depends on the db-backend
    seekable reader and is deferred to M17b; this box is therefore left partial
    `[~]` until that cheaper read path is realized.)

  Automated tests:
  - [X] Round-trip: writer emits `calls.dat` (+`calls.idx`), the Rust reader +
    ct-print read it back, call records match the combined stream.
    (M17a: `codetracer_trace_reader/tests/call_stream_tests.rs` —
    `calls_dat_matches_events_log` writes a nested-call trace with the flag on,
    reads `calls.dat` via the seekable `CallStreamReader`, and asserts the
    records equal the call tree re-derived from the unchanged `events.log`;
    `seek_to_call_by_key` seeks across a chunk boundary (chunk_size 2);
    `legacy_trace_has_no_call_stream` confirms a flag-off trace exposes no call
    stream and still reads.)
  - [X] db-backend seekable read: fetch a call record by `call_key` WITHOUT
    materializing the whole trace (assert bounded decompression / only the needed
    chunk is read), and verify concurrent-reader access.
    (M17b: `codetracer/src/db-backend/tests/seekable_call_stream_test.rs` —
    `fetch_call_by_key_decompresses_only_its_chunk` writes a 5-call,
    chunk_size-2 (⇒ 3-chunk) split bundle and asserts fetching one call inflates
    EXACTLY one chunk (via the `SeekableCallStream::chunk_decompressions()`
    counter, which observes the M17a reader's one-chunk cache) — so the
    bounded-decompression / not-fully-materialized property is PROVEN by a test,
    not just argued. `ctfs_reader_serves_call_tree_from_calls_dat` proves the
    `CTFSTraceReader` serves the tree via the seekable hooks;
    `seekable_and_materialized_call_trees_agree` proves the seekable tree equals
    the materialized one structurally; `concurrent_readers_over_same_ct` drives 8
    threads over the same `.ct`; `flag_off_trace_exposes_no_seekable_stream` +
    `real_legacy_ct_reads_unchanged_with_no_seekable_stream` (the real flag-off
    `ruby.ct`) prove backward compat. Plus `meta_dat.rs::accepts_has_call_stream_flag`
    and `call_stream_source` unit tests. The whole existing db-backend lib + bin
    suite (732 tests) stays green.)
  - [X] Engine extracts the SAME executed set from a split bundle and a legacy
    bundle; backward compat — a legacy (no calls.dat) `.ct` still reads.
    (M17a: `repro_ct_incremental/tests/t_call_stream_split.nim` — over a
    `ruby_split.ct` (flag set) and the legacy `ruby.ct` (flag clear) twin, the
    engine extracts an identical executed set and decides identically; the
    `t_live_full_suite.nim` driver auto-includes it and stays green.)

  Acceptance: materialized `.ct` carries a dedicated, spec-shaped, seekable
  `calls.dat` stream, read on-demand by the db-backend's seekable reader and by
  ct-print/the engine, with zero breakage to existing readers.

* M18 — `ct test --incremental` standalone integration (codetracer)
  :STATUS: complete

  Goal: bring runtime-dependency test selection into CodeTracer's `ct test` (§16.7)
  so it works WITHOUT reprobuild — discovery + baseline recording + executed-
  function extraction + skip/re-run — reusing the same algorithm the prototype
  proved.

  Deliverables:
  - [X] The engine algorithm (dependency discovery + shallow/deep hash + decide/
    record/cache) available to `ct test` (shared library or ported core).
    Done as a CLEAN VENDOR of the proven engine into codetracer at
    `codetracer/src/ct_test/incremental/` (engine.nim, ctfs_trace.nim,
    extractors.nim, trace_reader.nim, backends.nim, catalog.nim). The
    decision/record/cache/shallow/deep-hash algorithm is byte-for-byte the
    reprobuild prototype's; each file carries a provenance banner pointing at the
    reprobuild source so the two copies stay in sync. The vendored engine wires
    ONLY the SOURCE/interpreted (`tbSourceInterpreted`) and CTFS-interpreted
    (`tbSourceCtfs`) backends; the native (DWARF/instruction-byte) backend is
    LEFT WITH NIL STRATEGIES so it fails safe to a re-run (it is not needed for
    the M18 interpreted-language scope and would pull objdump/DWARF tooling).
  - [X] `ct test --incremental`: record a baseline trace per test via the
    appropriate production recorder (Python/Ruby → modern CTFS `.ct`), extract
    executed functions + per-function shallow hashes, and on a subsequent run
    skip tests whose executed functions are unchanged / re-run the rest.
    Implemented in `codetracer/src/ct_test/incremental_cli.nim`
    (`decideIncremental` core + `runIncremental` CLI wrapper) and wired into the
    `ct` dispatch (`codetracer/src/ct/codetracer.nim`): `ct test --incremental
    --language <python|ruby> --program <path> [--source-root DIR] [--cache PATH]
    [--id TESTID]`. The existing `ct test e2e ...` reprobuild-HCR surface is
    unchanged (only the explicit `--incremental` token is intercepted).
  - [X] Honest per-language support matching the recorder availability. Python
    and Ruby are wired and drive their production recorders in their own dev
    shells (build-siblings strategy). Native/compiled languages are rejected up
    front with a clear "deferred" message rather than silently skipped; the
    engine additionally gates the native backend to a fail-safe re-run. A
    recorder that cannot build/record on this host reports a LOUD captured
    diagnostic and exits non-zero (never a `unittest.skip`).

  Automated tests:
  - [X] An end-to-end `ct test --incremental` run over a small multi-function
    fixture asserts skip/re-run decisions, at least one interpreted language LIVE.
    `codetracer/src/ct_test/incremental_e2e_test.nim` writes a real 4-function
    Python program, records a LIVE CTFS baseline with the production maturin/PyO3
    recorder, and asserts: first run ⇒ run-fresh; no-op re-run ⇒ SKIP; edit an
    EXECUTED function (`used_b`) ⇒ RE-RUN naming exactly `used_b` (not the other
    executed functions); edit the UNEXECUTED `unused_d` ⇒ SKIP. PASSES on
    arm64-macOS (observed reports: "run (fresh baseline)" / "skipped (unchanged)"
    / "re-run (changed: … functions: used_b)" / "skipped (unchanged)").
  - [X] No regression to the existing `ct test` surface — only `--incremental` is
    intercepted; `ct test e2e ...` still routes to the unchanged
    `runE2eTestCli`. The full `ct` binary compiles with the M18 wiring.

  Acceptance: `ct test` performs trace-based incremental selection standalone,
  validated end-to-end for Python (live) on arm64-macOS. Ruby is structurally
  wired (same CTFS-interpreted path + production recorder) and shares the live
  code path; native/compiled languages are deferred (engine fail-safes them) and
  rejected honestly by the CLI.

* M19 — Documentation (codetracer book + reprobuild user guide + spec alignment)
  :STATUS: complete

  Goal: ship the user-facing documentation and correct the campaign docs to the
  spec-accurate framing.

  Deliverables:
  - [X] codetracer book: a `usage_guide/incremental-testing.md` page for
    `ct test --incremental` (the standalone, productized feature), added to the
    book `SUMMARY.md` TOC. Created at
    `codetracer/docs/book/src/usage_guide/incremental-testing.md`, linked under
    the Usage guide section (after Tracepoints). Covers what it does (skip a test
    when none of the functions it executed changed), the CLI
    (`ct test --incremental --language <python|ruby> --program <path> …`), the
    per-language reality (Python live, Ruby wired, native via instrumentation,
    fail-safe never-false-skip), and the executed-set + per-function-hash
    decision. `mdbook build` succeeds (no broken SUMMARY link).
  - [X] reprobuild user guide (docs/user-guide/incremental-testing.md): native
    row updated to LIVE-via-compile-time-instrumentation (`ct_instrument` facet);
    a new row notes the OS-level, recorder-independent Intel-PT/MCR path is
    PLANNED (needs a Linux PT/MCR host); the dedicated `calls.dat`/`calls.idx`
    seekable call stream and the standalone `ct test --incremental` are both
    documented. Status banner and "See also" updated accordingly.
  - [X] This milestones file + the engine README: corrected the residual stale
    "native = recorder CI red on main+dev" / "Linux-only RR" / "JS upstream
    break" framing in M13's acceptance + deliverable prose to the verified
    reality (JS live after the stale-sibling fix; native live via
    instrumentation; OS-level PT/MCR the only remaining native gate). The engine
    README's M13 section was already spec-accurate and is left as the canonical
    LIVE-vs-gated matrix.

  Automated tests:
  - [X] Docs build / link-check: `mdbook build` succeeds for the codetracer book
    with the new page and SUMMARY entry (alerts preprocessor optional/skipped).

  Acceptance: a user can read how to use the feature both standalone (`ct test`)
  and under reprobuild, and the campaign docs match the implemented reality.

* M20 — Bump ALL materialized recorders to the calls/steps split + verify their tests
  :STATUS: complete

  RESULT (2026-06-21): the calls/steps split is now emitted by DEFAULT by every
  in-scope materialized recorder, and a Nim-written split bundle is SEEKABLE by
  the Rust `CallStreamReader` (the db-backend's reader). Two writer fixes landed:

  1. Nim `calls.idx` gap CLOSED (`codetracer-trace-format-nim`):
     `call_stream.nim` now stores the call stream as a chunked-Zstd `calls.dat`
     + a companion `calls.idx` (byte-compatible with the Rust
     `CallStreamReader`'s `calls.idx` parser per seekable-zstd.md), replacing the
     pre-M20 `calls.dat`+`calls.off` VariableRecordTable (which the Rust reader
     could not index). The public reader/writer API is unchanged; the writer
     gained a `finalizeCallStream` close-time step (driven by
     `multi_stream_writer.close()`). The Nim READER is dual-format: it uses the
     chunked path when `calls.idx` is present and FALLS BACK to the legacy
     `calls.off` VariableRecordTable when it is absent, so pre-M20 (flag-clear)
     bundles still read byte-for-byte unchanged (verified: legacy `ruby.ct` reads
     4 calls; new split bundles read via the chunked seekable path).
     PROOF a Nim-written split bundle is seekable: M17b's
     `real_split_ct_serves_calls_seekably_with_bounded_decompression` (db-backend
     `seekable_call_stream_test`) NO LONGER SKIPS — it now asserts the bundle
     opens seekably (calls.idx present), fetches the last call by key, and
     inflates ≤1 chunk. `ruby_split.ct` was REGENERATED from the now-fixed Nim
     writer (a genuine native Ruby recording of `/tmp/live_demo.rb`, the twin of
     the legacy `ruby.ct`) and carries `has_call_stream` + `calls.dat` +
     `calls.idx`.

  2. Rust `CtfsTraceWriter` emits the split BY DEFAULT (`codetracer-trace-format`):
     `with_options` now sets `emit_call_stream: true`, so any direct
     `CtfsTraceWriter::new` user gets `calls.dat`+`calls.idx`+`has_call_stream`
     without an opt-in. `with_call_stream(false)` remains the disable lever (for
     a legacy-golden round-trip). Existing trace-format tests stay green.

  WRITER-PATH FINDING (verified 2026-06-21): the live materialized recorders
  Ruby, Python, JS, and shell/Bash ALL drive the **Nim** `multi_stream_writer`
  via `codetracer_trace_writer_nim::create_trace_writer(.., Ctfs)` /
  `NimTraceWriter` (FFI), NOT the Rust `CtfsTraceWriter`. So fix (1) is the
  load-bearing one for every live recorder; fix (2) is belt-and-suspenders for
  any direct Rust-writer consumer.

  Goal (owner request 2026-06-21): the `calls.dat`/`steps.dat` split (M17) is only
  truly delivered when EVERY materialized-trace recorder emits it and each
  recorder's own test suite is verified green with the split. M17a added the
  capability to the writers; this milestone propagates it to all recorders and
  validates them end-to-end as part of the campaign.

  Context (verified 2026-06-21): the live recorders (Ruby, Python, JS, shell)
  drive the **Rust `CtfsTraceWriter`**, which currently emits `calls.dat` only when
  the caller opts in via `with_call_stream(true)` — so by default their `.ct`
  bundles do NOT carry `calls.dat`. The Nim `multi_stream_writer` sets
  `has_call_stream = true` unconditionally (multi_stream_writer.nim:1092). Which
  writer path each recorder uses at RUNTIME must be confirmed per recorder; the
  bump makes each one emit the split.

  Materialized recorders in scope (confirm the full set against
  codetracer-trace-format-spec/internal-files.md §"Runtime Tracing (DB Traces)"):
  codetracer-ruby-recorder, codetracer-python-recorder, codetracer-js-recorder,
  codetracer-shell-recorders (Bash), codetracer-wasm-recorder,
  codetracer-wasmi-recorder, and any Noir path. (RR/native MCR recordings are NOT
  materialized DB traces and are out of scope here.)

  Deliverables:
  - [X] Determine each recorder's actual writer path and make each emit
    `calls.dat`+`calls.idx` + set `has_call_stream`. FINDING: Ruby, Python, JS,
    and shell/Bash all drive the Nim `multi_stream_writer` (via
    `codetracer_trace_writer_nim`'s FFI `create_trace_writer`/`NimTraceWriter`);
    the Nim `calls.idx` fix makes them all emit the split (the writer already set
    the flag unconditionally). The Rust `CtfsTraceWriter` now defaults
    `emit_call_stream=true` too, covering any direct Rust-writer consumer. No
    recorder is left on the flag-off path.
  - [X] For each recorder, a LIVE recording produces a valid `calls.dat`+`calls.idx`
    that ct-print reads (executed set identical to the `events.log`-derived set):
    * Ruby — `/tmp/live_demo.rb` ⇒ has_call_stream + calls.idx, ct-print reads 4
      calls (set {<top-level>, main, used_a, used_b}); IS the `ruby_split.ct`
      fixture, proven seekable by the db-backend reader.
    * Python — a 3-function demo ⇒ has_call_stream + calls.idx (no calls.off),
      ct-print reads 4 calls.
    * JavaScript — a 3-function demo ⇒ has_call_stream + calls.idx (no calls.off),
      ct-print reads 4 calls.
    * shell/Bash — driven via the wire protocol ⇒ has_call_stream + calls.idx (no
      calls.off), ct-print reads 3 calls.
  - [X] Run EACH recorder's OWN test suite (Ruby/Python/JS/shell) and verify it
    stays green with the split:
    * Python — `just cargo-test` (149 Rust tests) + `just py-test` (135 Python
      tests) ALL GREEN with the split.
    * Ruby — recorder test suite is SPLIT-NEUTRAL: 45 runs / 5 failures both WITH
      and WITHOUT the M20 Nim change (proven by stash+rebuild A/B); the 5 are
      pre-existing golden drifts in `events.log` value/ARGV encoding (e.g.
      args_sum ARGV-array splitting, return values), NOT calls.dat/has_call_stream.
      (`test_rack_integration.rb` additionally errors on a missing `rack` gem —
      an env gap.) No split-induced regression.
    * JavaScript — 264 passed / 2 failed; the 2 (`transitive_composition`
      sourcemap+rename) are PRE-EXISTING (identical on the clean baseline via
      stash+rebuild A/B), orthogonal to the call stream.
    * shell/Bash — unit tests green; 3 `bash_recording_test` e2e cases fail on a
      PRE-EXISTING `trace_db_metadata.json` control-character JSON bug (identical
      on the clean baseline), unrelated to the split.
  - [~] Bump each recorder's pinned `codetracer-trace-format` / `-nim` sibling.
    DEFERRED to the review-agent COMMIT step (no git commits in this milestone per
    the task): the pins must reference the POST-M20 commit SHA of
    `codetracer-trace-format` / `-nim`, which does not exist until the changes are
    committed. The LIVE verification above already built every recorder against
    the LOCAL M20 sibling (nix `--override-input` / cargo path dep), so the proof
    used the actual M20 writer regardless of the pin. Recorders WITHOUT a
    trace-format pin (shell/wasm/wasmi) need no bump.

  Other materialized recorders (deliverable 5, honest status):
  - [~] codetracer-wasm-recorder, codetracer-wasmi-recorder — these checkouts are
    the UPSTREAM runtime sources (wazero / wasmi) with NO codetracer trace-writer
    integration present (no `codetracer-trace-format` dependency, no `.ct`
    emission). Nothing to bump; structurally out of scope until a recorder glue
    layer exists. When one is added it will (per the codetracer convention) drive
    the same Nim/Rust writer and thus inherit the split automatically.
  - [~] Noir path — no materialized-DB Noir recorder drives a `calls.dat` writer
    in this workspace; nothing to bump here. Any future Noir materialized recorder
    going through the Nim/Rust writer inherits the split by default.

  Automated tests:
  - [X] Per recorder: a live-recording check asserting the produced `.ct` has
    `has_call_stream` + a readable `calls.dat`+`calls.idx` (done above for
    Ruby/Python/JS/shell; calls.idx presence verified via base40 file-table scan;
    Ruby proven seekable end-to-end by the db-backend reader).
  - [X] The campaign full live suite (`t_live_full_suite.nim`) stays GREEN with
    the split-emitting recorders; the M17a engine split test
    (`t_call_stream_split.nim`) is GREEN over the regenerated Nim-writer
    `ruby_split.ct`.

  Acceptance: every in-scope materialized recorder emits the calls/steps split
  (Nim writer fix + Rust default), each recorder's suite is green or
  split-neutral with documented pre-existing failures, and a live `.ct` from each
  is read via `calls.dat`/`calls.idx` end-to-end — Ruby additionally proven
  SEEKABLE through the db-backend reader (M17b test un-skipped).

  db-backend `seekable_call_stream_test` (REVIEW-VERIFIED 2026-06-21): all 7
  tests PASS in the delivered tree, including
  `real_legacy_ct_reads_unchanged_with_no_seekable_stream` (legacy flag-off
  `ruby.ct` reads its call tree on the materialized path — ct-print reports 4
  calls, `call_count() >= 1` holds) AND the M17b
  `real_split_ct_serves_calls_seekably_with_bounded_decompression` (now
  UN-SKIPPED — the regenerated Nim-written `ruby_split.ct` opens seekably via
  its `calls.idx`). An earlier draft of this note claimed a "0 calls" legacy
  regression; that does NOT reproduce on the delivered working tree and the
  claim is withdrawn. The committed legacy `ruby.ct` fixture is byte-unchanged.

  Pre-existing, OUT-OF-SCOPE failures observed (NOT introduced by M20; do not
  touch `calls.dat`/`has_call_stream`; left untouched, not masked):
  - db-backend `elixir_flow_dap_test` aborts when the BEAM/Elixir recorder
    sibling + fixture are absent (`CODETRACER_BEAM_RECORDER_PATH` /
    `CODETRACER_ELIXIR_FLOW_TEST` unset) — a workspace/env gap, unrelated to the
    call-stream split.
  - Ruby 5 golden drifts; JS 2 transitive_composition; shell 3 bash e2e
    (metadata JSON) — all pre-existing as detailed above.

# =============================================================================
# Phase 5 — Follow-ups (owner: "implement all follow-ups", 2026-06-21)
# =============================================================================
# The follow-ups recorded at the end of Phase 4: (M21) the M18 engine is vendored
# into codetracer as a second physical copy — guarantee a single source of truth;
# (M22) retire the in_memory_trace_reader full-load by making the step/value
# streams seekable too (completes the M17b spec-violation fix); (M23) finish the
# trace-events.md "Event Stream Redesign" beyond calls.dat (compact step encoding,
# value stream, events.dat, varint interning). M16 (Intel PT) stays deferred to an
# x86+PT host (owner-approved) and is NOT part of Phase 5.

* M21 — Single source of truth for the incremental engine (de-duplicate the vendor)
  :STATUS: complete

  Goal: M18 vendored the engine core from reprobuild's `repro_ct_incremental` into
  `codetracer/src/ct_test/incremental/` as a SECOND physical copy (faithful, with
  provenance banners). Two copies can silently drift. Establish a single source of
  truth so a change to the algorithm cannot diverge between the two consumers.

  Design constraint: `codetracer` must NOT acquire a build/runtime dependency on
  `reprobuild` being present (codetracer ships standalone), and `reprobuild` must
  NOT depend on `codetracer` — so a straight "move it to one repo and import across"
  is rejected in BOTH directions. The pragmatic single-source-of-truth that
  respects both independences: keep `reprobuild/libs/repro_ct_incremental` as the
  CANONICAL source, treat the codetracer copy as GENERATED, and ENFORCE identity.
  (A physical relocation into a shared package is a separate team architecture
  decision, noted but out of scope.)

  Deliverables:
  - [X] A deterministic sync mechanism: `tools/sync_ct_test_vendor.nim` (+ the
    `tools/sync_ct_test_vendor.sh` dev-shell wrapper) regenerates the codetracer
    vendored copy from the canonical reprobuild source by MECHANICAL transforms
    only: it prepends each module's provenance banner (data files under
    `tools/banners/`, with `{{PROVENANCE}}` bound to the canonical path) and
    applies the documented `engine.nim` interpreted-only trim. The trim is
    expressed by inert `#@ctvendor strip/replace/with/end` marker comments living
    in the CANONICAL `engine.nim` (so canonical still compiles + its tests pass),
    plus one auditable global literal sub `.repro-ct-incremental` → `.ct-incremental`
    (the codetracer cache-dir name). No hand edits; idempotent (a second run on an
    in-sync tree yields no diff). The codetracer checkout defaults to the workspace
    sibling, overridable by arg or `$CODETRACER_CHECKOUT`.
  - [X] A DRIFT-CHECK test `tests/t_ct_test_vendor_sync.nim` lives in reprobuild
    (sees both trees via the workspace sibling, like `live_record.WorkspaceRoot`).
    It runs the generator in `--check` mode against the REAL codetracer checkout
    and fails if any vendored file differs from `sync(canonical)`; it is auto-run
    by the campaign suite (`t_live_full_suite` walks every `t_*.nim`). NOT a silent
    skip — a missing checkout/vendored dir FAILS with a clear diagnostic. It also
    self-tests the detector: it copies the vendored tree to a scratch dir, perturbs
    `engine.nim`, and asserts `--check` then exits non-zero (so the pass is not
    vacuous), never touching the real tree.
  - [X] Regenerated the codetracer copy via the script. The four self-contained
    modules + `backends.nim` regenerate BYTE-IDENTICAL to the M18 committed copy.
    `engine.nim` is an EXPLAINED IMPROVEMENT: the regenerated form keeps the
    canonical's CURRENT (richer) doc/inline comments instead of M18's hand-shortened
    ones and adds the missing `currentDepLocations` rebind comments — same
    algorithm, just the canonical text — and the committed codetracer `engine.nim`
    was updated to this generated form. Both consumers build + test green.

  Automated tests:
  - [X] The drift-check passes on the in-sync tree, and FAILS when a vendored file
    is deliberately perturbed — proven both via the test's embedded scratch
    self-test and by perturbing the REAL vendored `engine.nim` (the `--check`
    wrapper exited 1 naming `engine.nim`; regenerating restored byte-identity).
  - [X] reprobuild engine suite (`t_live_full_suite`) green WITH the new
    drift-check; codetracer `incremental_e2e_test` green against the regenerated
    engine.

  Acceptance: the algorithm has ONE canonical home; the codetracer copy is
  mechanically generated and drift-guarded, so the two cannot silently diverge.

* M22 — Retire the in_memory_trace_reader full-load (seekable step/value streams)
  :STATUS: planned

  Goal: complete the M17b spec-violation fix. M17b made the CALL tree seekable
  (`calls.dat`); the step/value/cell lookups still materialize a full in-memory
  `Db` (`in_memory_trace_reader.rs`), and `diff.rs` still uses it. Make the
  execution + value streams seekable too, so the db-backend never fully
  materializes a (large/network-loaded) `.ct`, and the full-load reader is retired
  (or bounded to an explicitly-labelled tiny-trace/test fallback only).

  This depends on the materialized `.ct` carrying seekable `steps.dat` (+ value)
  streams per trace-events.md (see M23) — so M22 and M23 are coordinated: M23
  produces the seekable step/value streams; M22 consumes them in the db-backend
  and routes the production read paths (DAP step/value/cell handlers, `diff.rs`)
  through the seekable reader, retiring the `Db` full-load.

  Deliverables:
  - [ ] db-backend reads steps/values/cells on-demand from the seekable
    `steps.dat` (+ value) streams via CTFS block seeking + the companion index +
    an LRU, never the whole stream into memory.
  - [ ] Route the remaining production read paths (DAP handlers, `diff.rs`, the
    `dap_server` placeholder) off `InMemoryTraceReader`; retire it or bound it to a
    clearly-labelled small-trace/test fallback.
  - [ ] Bounded-decompression PROVEN by a counter test (as M17b did for calls),
    and concurrent-reader + backward-compat tests; existing db-backend tests green.

  Automated tests:
  - [ ] Fetch a step/value by id with bounded decompression (counter-proven, not
    argued); concurrent readers; legacy bundle reads unchanged.

  Acceptance: no production read path fully materializes a `.ct`; the full-load
  `Db` is retired/bounded; the seekable reader is the network/large-trace path.

* M23 — Finish the trace-events.md Event Stream Redesign (steps/value/events.dat)
  :STATUS: M23a complete (steps.dat compact seekable execution stream) and M23b
  complete (values.dat parallel value stream — writer + seekable reader +
  ct-print flag + meta.dat flag across Rust/Nim/db-backend +
  round-trip/parallel-index/seek/bounded-decompression tests). M23c+ (events.dat,
  varint interning, events.log retirement) PENDING. M23 NOT complete.

  Goal: implement the remainder of `codetracer-trace-format-spec/trace-events.md`'s
  "Event Stream Redesign" beyond the `calls.dat` split already shipped: the compact
  execution stream (`steps.dat` with AbsoluteStep/DeltaStep), the parallel value
  stream, the I/O `events.dat`, and varint interning tables (`paths.dat`/`funcs.dat`/
  `types.dat`/`varnames.dat`) — replacing the legacy single combined `events.log`.

  SCOPE WARNING: this is the deepest, product-WIDE change — it touches the writer
  (all recorders), every reader (db-backend, ct-print, reprobuild engine, visual
  replay), and the GUI. It MUST be staged + additive/gated like the calls.dat split
  (M17a), never a flag-day break. Decompose during execution into sub-milestones,
  one stream at a time, each additive behind a `meta.dat` capability flag with
  backward-compatible readers and round-trip + seekable tests, validated across the
  affected recorders before the next. Treat each sub-milestone with the same
  implement→adversarial-review→commit process. (Sequencing: `steps.dat` first — it
  unblocks M22 — then the value stream, then `events.dat`, then varint interning,
  then retiring `events.log` once every reader consumes the split streams.)

  Deliverables (high-level; refined per sub-milestone):
  - [X] (M23a) `steps.dat` compact seekable execution stream
    (AbsoluteStep/DeltaStep + Raise/Catch/ThreadSwitch) + `steps.idx`, additive
    behind the new `meta.dat` flag bit 9 `has_step_stream`; round-trip + seek +
    bounded-decompression tests; unblocks M22.
  - [X] (M23b) Parallel value stream (`values.dat` + `values.idx`),
    parallel-indexed to `steps.dat` (value record N ↔ step N, empty record for
    value-less steps), additive behind the new `meta.dat` flag bit 10
    `has_value_stream`; round-trip + parallel-index + seek + bounded-decompression
    tests; consumer migration deferred to M22.
  - [ ] I/O `events.dat`; varint interning tables.
  - [ ] Retire the legacy `events.log` only AFTER every reader consumes the split
    streams; backward-compat for old bundles retained.

  ** M23a — steps.dat compact seekable execution stream (DONE)
     :STATUS: complete

     The first sub-milestone of M23, mirroring the M17a `calls.dat` split
     exactly: a dedicated compact, seekable execution stream split additively out
     of the unified `events.log`, gated behind a new `meta.dat` capability flag,
     WITHOUT migrating any reader's step consumption (the GUI/db-backend keeps
     reading `events.log` — that consumer migration is M22). `events.log` is
     unchanged byte-for-byte; old readers ignore `steps.dat`/`steps.idx`.

     Deliverables:
     - [X] Writer (`codetracer-trace-format/codetracer_trace_writer`): a new
       `step_stream.rs` module derives, from the SAME Step/Call/Return/ThreadSwitch
       events that feed `events.log`, a compact `steps.dat` execution stream
       (AbsoluteStep tag 0 / DeltaStep tag 1 + Raise/Catch/ThreadSwitch tags 2/3/4
       per trace-events.md §"Compact Step Encoding") as chunked-Zstd with a
       companion `steps.idx`. Encoding rules: first step + post-call + post-return
       (+ post-thread-switch) are AbsoluteStep; first record of every chunk is
       AbsoluteStep so chunks decode independently; others DeltaStep when the
       signed zigzag delta fits ±1048575. `global_line_index` is derived by a
       reversible `(path_id, line)` packing (the canonical `funcs.dat` interning is
       M23b+). Emission is OPT-IN via `CtfsTraceWriter::with_step_stream(true)` and
       gated behind the new `meta.dat` flag `FLAG_HAS_STEP_STREAM` (bit 9). ADDITIVE:
       `events.log` UNCHANGED (a byte-identity test proves it); the call-stream and
       step-stream flags coexist in one `meta.dat`.
     - [X] Seekable reader (`codetracer_trace_reader`): a new
       `step_stream_reader.rs` `StepStreamReader` honoring the `has_step_stream`
       flag, seeking to step N by chunk (decompresses only that chunk), decoding
       AbsoluteStep/DeltaStep back to absolute `global_line_index` (running absolute
       resets per chunk). Mirrors `CallStreamReader`'s shape (count / chunk_size /
       cached_chunk probe / read / read_all). A legacy (flag-off) `.ct` exposes no
       step stream and still reads.
     - [X] ct-print mirror (`codetracer-trace-format-nim`): the `has_step_stream`
       flag is surfaced in `--meta-json` and `--full` (alongside `has_call_stream`);
       `--json-events` output UNCHANGED; the nim `meta_dat.nim` writer/reader knows
       bit 9 (`FlagHasStepStream`) and includes it in `KnownFlags` so a split bundle
       opens.
     - [X] meta_dat new-flag in EVERY known-flags mask: Rust trace-format
       (`meta_dat::FLAG_HAS_STEP_STREAM`), Nim (`FlagHasStepStream` + `KnownFlags`),
       AND the db-backend `ctfs_trace_reader::meta_dat` (bit 9 added to
       `KNOWN_FLAGS_MASK`), so a `has_step_stream` bundle still OPENS in the GUI
       (the M17b diligence — db-backend rejecting an unknown flag — repeated here).

     Automated tests (all green on arm64-macOS):
     - [X] Rust round-trip (`step_stream_tests.rs::steps_dat_matches_events_log`):
       the decoded absolute `global_line_index` sequence EQUALS the Step sequence
       derived from `events.log` (small chunk_size so steps span multiple chunks).
     - [X] Seek across a chunk boundary
       (`seek_to_step_across_chunk_boundary`): chunk_size 4, seek into a later
       chunk and back into chunk 0, out-of-range errors not panics.
     - [X] Bounded decompression
       (`fetching_one_step_decompresses_only_its_chunk`): counter-proven via the
       reader's `cached_chunk()` probe — fetching one step inflates only its own
       chunk (the M17b calls bounded-decompression analogue).
     - [X] Backward compat: `events_log_byte_identical_with_and_without_step_stream`
       (events.log byte-for-byte identical) + `legacy_trace_has_no_step_stream`
       (flag-off ⇒ no step stream, still reads).
     - [X] meta_dat known-flag acceptance in all three parsers: Rust
       `meta_dat::tests::meta_dat_step_stream_flag_roundtrip`, db-backend
       `accepts_has_step_stream_flag` (step-alone AND call+step), Nim
       `test_meta_dat_strict_unknown_flag_rejection` (bit 9 round-trips; the
       unknown-bit probe retargeted to bit 10). Nim `ct_print_full` golden updated
       to carry `has_step_stream: false`.

     Acceptance: a compact seekable `steps.dat` execution stream ships additively
     behind `has_step_stream`, consistent with `events.log` (which is unchanged),
     read seekably by the Rust `StepStreamReader`; every meta_dat parser accepts
     the new flag so a split bundle opens in the GUI. Value stream / events.dat /
     varint interning / events.log retirement are LEFT for M23b+.

  ** M23b — values.dat parallel value stream (DONE)
     :STATUS: complete

     The second sub-milestone of M23, mirroring the M23a `steps.dat` split and
     the M17a `calls.dat` split exactly: a dedicated, seekable parallel VALUE
     stream split additively out of the unified `events.log`, gated behind a new
     `meta.dat` capability flag, WITHOUT migrating any reader's value consumption
     (the GUI/db-backend keeps reading `events.log` — that consumer migration is
     M22). `events.log` is unchanged byte-for-byte; old readers ignore
     `values.dat`/`values.idx`.

     File-naming decision: trace-events.md's summary table labels BOTH the
     execution and value streams `steps.dat`, but execution records are tiny
     (2-4 B) and value records are large (50-500 B) with different Zstd chunk
     sizing, and a CTFS internal file is a single seekable byte range with one
     companion `.idx` — so two streams cannot share one file. The value stream
     therefore ships as its OWN CTFS file pair `values.dat` + `values.idx`,
     PARALLEL-INDEXED to `steps.dat` (value record N ↔ step N, empty record for
     value-less steps). This decision + the parallel-index invariant are
     documented in `value_stream.rs`/`value_stream_reader.rs` module docs, the
     Nim `FlagHasValueStream` doc-comment, the db-backend `FLAG_HAS_VALUE_STREAM`
     doc-comment, and an implementation note in
     `codetracer-trace-format-spec/trace-events.md` §"Value Stream".

     Deliverables:
     - [X] Writer (`codetracer-trace-format/codetracer_trace_writer`): a new
       `value_stream.rs` module derives, from the SAME value events that feed
       `events.log` (StepValues from `Value`, plus BindVariable / DropVariable /
       DropVariables / CellValue / CompoundValue / AssignCell / AssignCompoundItem
       / VariableCell / Assignment — the value-stream tag set per trace-events.md),
       a `values.dat` parallel value stream as chunked-Zstd with a companion
       `values.idx`. Records are parallel-indexed to the step stream; each per-step
       record reuses the existing `ValueRecord`/`RValue` CBOR encoding from
       `codetracer_trace_types` for value payloads (NOT redesigned). Emission is
       OPT-IN via `CtfsTraceWriter::with_value_stream(true)` and gated behind the
       new `meta.dat` flag `FLAG_HAS_VALUE_STREAM` (bit 10). ADDITIVE: `events.log`
       UNCHANGED (a byte-identity test proves it); the call/step/value-stream flags
       coexist in one `meta.dat`.
     - [X] Seekable reader (`codetracer_trace_reader`): a new
       `value_stream_reader.rs` `ValueStreamReader` honoring the `has_value_stream`
       flag, fetching step N's value record by chunk (decompresses only that
       chunk). Mirrors `StepStreamReader`'s shape (count / chunk_size /
       cached_chunk probe / read / read_all). A legacy (flag-off) `.ct` exposes no
       value stream and still reads.
     - [X] ct-print mirror (`codetracer-trace-format-nim`): the `has_value_stream`
       flag is surfaced in `--meta-json` and `--full` (alongside `has_call_stream`
       / `has_step_stream`); `--json-events` output UNCHANGED; the nim
       `meta_dat.nim` writer/reader knows bit 10 (`FlagHasValueStream`) and
       includes it in `KnownFlags` so a value-split bundle opens.
     - [X] meta_dat new-flag in EVERY known-flags mask: Rust trace-format
       (`meta_dat::FLAG_HAS_VALUE_STREAM`), Nim (`FlagHasValueStream` +
       `KnownFlags`), AND the db-backend `ctfs_trace_reader::meta_dat` (bit 10
       added to `KNOWN_FLAGS_MASK`), so a `has_value_stream` bundle still OPENS in
       the GUI (the M17b/M23a diligence repeated here).

     Automated tests (all green on arm64-macOS):
     - [X] Rust round-trip (`value_stream_tests::values_dat_matches_events_log`):
       the per-step value records decoded from `values.dat` EQUAL the records
       re-derived from `events.log` via the same `ValueStreamBuilder` (small chunk
       size so records span multiple chunks); parallel-index invariant asserted
       (record count == step count, empty records present, StepValues present).
     - [X] Seek across a chunk boundary
       (`seek_to_step_values_across_chunk_boundary`): chunk_size 4, seek into a
       later chunk and back into chunk 0, value-less step decodes empty,
       out-of-range errors not panics.
     - [X] Bounded decompression
       (`fetching_one_step_decompresses_only_its_chunk`): counter-proven via the
       reader's `cached_chunk()` probe — fetching one step's values inflates only
       its own chunk.
     - [X] Backward compat:
       `events_log_byte_identical_with_and_without_value_stream` (events.log
       byte-for-byte identical) + `legacy_trace_has_no_value_stream` (flag-off ⇒
       no value stream, still reads).
     - [X] meta_dat known-flag acceptance in all three parsers: Rust
       `meta_dat::tests::meta_dat_value_stream_flag_roundtrip`, db-backend
       `accepts_has_value_stream_flag` (value-alone AND call+step+value), Nim
       `test_meta_dat_strict_unknown_flag_rejection` (bit 10 round-trips; the
       unknown-bit probe retargeted to bit 11). Nim `ct_print_full` golden updated
       to carry `has_value_stream: false`.

     Acceptance: a seekable `values.dat` parallel value stream ships additively
     behind `has_value_stream`, consistent with `events.log` (which is unchanged)
     and parallel-indexed to `steps.dat`, read seekably by the Rust
     `ValueStreamReader`; every meta_dat parser accepts the new flag so a split
     bundle opens in the GUI. events.dat / varint interning / events.log
     retirement are LEFT for M23c+.

  Acceptance: materialized `.ct` is the spec's multi-stream format end-to-end,
  read seekably by every consumer, with no regression and a clean legacy-read path.
