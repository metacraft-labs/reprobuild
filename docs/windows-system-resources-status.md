# Windows-System-Resources implementation status

Tracks the per-phase status of the spec at
`metacraft-labs/reprobuild-specs/Windows-System-Resources.md` (merged
as commit `f030bf512824e7b4287b0d31632d6526377d9265`).

This file is **append-only per phase** — the implementation agent
updates the relevant row when a phase moves to "implemented (awaiting
review)"; the review agent updates it again to "reviewed + committed"
once the commit lands. Do not delete or rewrite past rows.

## Phase status

| # | Phase | Status | Commit | Notes |
|---|-------|--------|--------|-------|
| A | `fs.systemFile` external sources (`sourceUrl + sha256`, `sourceLocal`) | reviewed + committed | `d7ea49c8` | Ran all five spec-mandated test files; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 104 OK / 0 FAILED. `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 163 OK / 0 FAILED. `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 273 OK / 0 FAILED. `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 12 OK / 0 FAILED. `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 9 OK / 0 FAILED. Reviewer reproduced all five pass-counts and ran the wider regression sweep across `libs/repro_profile/tests/`, `libs/repro_infra/tests/`, `libs/repro_elevation/tests/`, `libs/repro_profile_compile/tests/` (incl. `t_m2_nixos_darwin_modules`, `t_sandbox_m1_fhssandbox_driver`, `t_smoke_apply_integration`, `t_smoke_module_imports`, `t_smoke_profile_adapter`, `t_smoke_repro_profile_compile`, `t_template_in_template_named_args`) — no regressions. |
| B | `windows.service` schema extension (displayName, binPath, recoveryActions, recoveryResetSeconds) | reviewed + committed | `23c54537` | Ran all five spec-mandated test files; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 113 OK / 0 FAILED. `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 173 OK / 0 FAILED. `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 295 OK / 0 FAILED. `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 16 OK / 0 FAILED. `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 10 OK / 0 FAILED. Reviewer reproduced all five pass-counts and ran the wider regression sweep across `libs/repro_profile/tests/`, `libs/repro_infra/tests/`, `libs/repro_elevation/tests/`, `libs/repro_profile_compile/tests/` (incl. `t_m2_nixos_darwin_modules` 20 OK, `t_sandbox_m1_fhssandbox_driver` 35 OK, `t_smoke_apply_integration` 3 OK, `t_smoke_module_imports` 12 OK, `t_smoke_profile_adapter` 88 OK, `t_smoke_repro_profile_compile` 16 OK, `t_template_in_template_named_args` 2 OK) — no regressions. |
| C | `windows.scheduledTask` new resource | reviewed + committed | `23c18351` | All five spec-mandated test files run cleanly; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 133 OK / 0 FAILED. `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 185 OK / 0 FAILED. `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 322 OK / 0 FAILED. `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 20 OK / 0 FAILED. `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 11 OK / 0 FAILED. Fix applied after first review pass: `windowsScheduledTask` template `runWithHighestPrivileges` is now sentinel-aware (parameter type `Option[bool]`, default `none(bool)`) so the text parser's principal-dependent default applies when unset. Added three tests covering LOCAL_SERVICE default -> false, SYSTEM default -> true, SYSTEM explicit-false round-trip. (Per-suite pass-counts above reflect post-fix totals: +3 in `t_smoke_repro_profile` template-surface tests, +3 in `t_smoke_repro_infra` end-to-end tests.) Reviewer reproduced all five pass-counts and ran the wider regression sweep across `libs/repro_profile/tests/`, `libs/repro_infra/tests/`, `libs/repro_elevation/tests/`, `libs/repro_profile_compile/tests/` (incl. `t_m2_nixos_darwin_modules` 20 OK, `t_sandbox_m1_fhssandbox_driver` 35 OK, `t_smoke_apply_integration` 3 OK, `t_smoke_module_imports` 12 OK, `t_smoke_profile_adapter` 88 OK, `t_smoke_repro_profile_compile` 16 OK, `t_template_in_template_named_args` 2 OK) — no regressions. Reviewer audit confirmed: zero pre-existing test assertions were modified (1235 insertions / 0 deletions across all test files); the fix is pure addition. |
| D | `fs.systemDirectory` ACL apply (carry-over from reprobuild#7's TODO) | reviewed + committed | `2228c183` | All five spec-mandated test files run cleanly; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 137 OK / 0 FAILED. `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 189 OK / 0 FAILED. `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 344 OK / 0 FAILED. `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 21 OK / 0 FAILED. `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 12 OK / 0 FAILED. New m83 fixture `tests/fixtures/m83/system_fssystemdirectory_acl.nim` covers every documented `aclInheritance` value, the Allow + Deny ACE pivot, and an owner-unset stanza. Phase D shipped pure argv assemblers (`renderTakeownDirCommandArgs`, `renderIcaclsSetOwnerCommandArgs`, `renderIcaclsInheritanceCommandArgs`, `renderIcaclsGrantCommandArgs`) + the `splitDirAclEntry` Allow/Deny splitter + the drift digest contribution (`canonicalDirAclDesired`, `canonicalDirAclObserved`, `dirAclMatchesDesired`, `fsSystemDirectoryDigestPayload`, `normalizeDirAclEntry`) in `posix_system_parse.nim`. `observeFsSystemDirectory` now folds the live `icacls <path>` observation into the digest when `dirAclPresent == true` (cross-platform pure when false, byte-identical to PR #7's legacy `BLAKE3("fs.systemDirectory:present")` form). `applyFsSystemDirectory` reuses the pure assemblers via `renderArgvAsShellCmd` for the Windows shell-out + adds a strict post-apply re-probe asserting the live ACL digest matches desired. Reviewer reproduced all five pass-counts and ran the wider regression sweep across `libs/repro_profile/tests/`, `libs/repro_infra/tests/`, `libs/repro_elevation/tests/`, `libs/repro_profile_compile/tests/` (incl. `t_m2_nixos_darwin_modules` 20 OK, `t_sandbox_m1_fhssandbox_driver` 35 OK, `t_smoke_apply_integration` 3 OK, `t_smoke_module_imports` 12 OK, `t_smoke_profile_adapter` 88 OK, `t_smoke_repro_profile_compile` 16 OK, `t_template_in_template_named_args` 2 OK) — no regressions. Reviewer audit confirmed: zero pre-existing test assertions were modified (568 insertions / 0 deletions across all test files); the new tests are pure addition. Spec said "PR #7's stub" but PR #7 was not a stub — icacls was actually invoked inline; the implementer correctly identified + closed three real gaps (no pure argv assemblers, no ACL in observation digest, no post-apply ACL re-probe). LoC: spec said ~100, landed ~363 production LoC — the expansion is justified gap-filling (pure assemblers + canonical-digest helpers + drift comparison), not gold-plating. The `protected-clear-inherited` ↔ `disabled-replace` distinction is real: the narrower `windows.acl` validator rejects `protected-clear-inherited`; only the wider `DirectoryAclInheritanceModes` set accepts it; the codec round-trip preserves the operator's choice + each value digests distinctly. Parallel `canonicalDir*` helpers (vs reusing `windows.acl`'s `canonicalAcl*`) are justified: wider inheritance vocabulary, cross-platform driver, and the canonical-key prefix differs (`dirAcl:...` vs `acl:<path>:...`) so a stray shared call cannot accidentally collide. |
| E | Build-engine `requiresElevation = true` edge attribute + `@FILE:<path>` argv preprocessor | reviewed + committed | `6b324294` | All five spec-mandated test files run cleanly; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 137 OK / 0 FAILED. `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 189 OK / 0 FAILED. `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 378 OK / 0 FAILED. `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 22 OK / 0 FAILED. `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 13 OK / 0 FAILED. Plus NEW Phase E DSL test file `libs/repro_dsl_stdlib/tests/t_smoke_phase_e_requires_elevation.nim`: 7 OK / 0 FAILED (pins `BuildActionDef.requiresElevation` defaults + `buildAction(..., requiresElevation = true)` parameter + codec round-trip in both directions + the full `inlineExecCall + requiresElevation = true` spec example). Phase E shipped `pokInlineExecCall` (new PrivilegedOperation kind in `operations.nim` with closed-set validator, kind-tag helpers, RBEB protocol encode/decode, dispatch-layer spawn path), the `expandArgFiles` / `expandArgFileToken` / `auditArgvWithRedaction` / `argFileTokenError` pure helpers (in `operations.nim`), the `inline_exec_driver.nim` module (`runInlineExecCall`, `expandExecCallArgv`, `inlineExecCallAuditDetail`, `defaultArgFileReader`, `ArgFileReader` typed injection seam, `InlineExecCallOutcome`), the `requiresElevation` edge attribute on `BuildActionDef` (DSL side) + `BuildAction` (engine side) with codec v18 -> v19 bump (sentinel byte + back-compat decoder for v18-and-earlier), the `lowerGraphAction` inline-exec branch's `@FILE:` expander + `requiresElevation` propagation. Decision points: (a) `@FILE:` relative-path REFUSED at the codec boundary — the broker's cwd is not a stable identity, so a `@FILE:secret.tok` from a non-deterministic cwd raises `EProtocol` rather than silently reading whatever happens to be at that name; (b) `pokInlineExecCall` bypasses the observe/desired-digest dispatch model — `dispatchOperation` special-cases it and calls `runInlineExecCall` before the reobserve/drift gate since a one-shot spawn has no observable steady state; (c) the @FILE: expansion at lowering time vs at fork time — landed at lowering time inside `lowerGraphAction` because the engine's spawn primitive is one step further down and the elapsed time between lowering and fork is single-digit milliseconds inside the same apply (spec said "at the moment of execution" — lowering IS the moment of execution from the apply's perspective); (d) `isArgFileToken("@FILE:")` returns true so the validator catches the bare-prefix case rather than silently passing it through as a literal arg. Two existing tests had `trimBytes` updated for the v19 codec growth (`libs/repro_project_dsl/tests/t_engine_typed_output_payload_codec_round_trip.nim` — `older v11 payload` trim of 18 -> 19 bytes; `older v16 payload` trim of 4 -> 5 bytes; the post-fix tests assert v18-and-earlier payloads decode with `requiresElevation = false` so the back-compat sentinel is positively pinned). **Phase E fix-up (post first-review):** the scheduler-side broker dispatch wiring that the first pass had left as a doc-only promise is now actually present. The architectural seam chosen is the **pre-launch decision point** (reviewer's Option A): a single new branch inside `runBuild`'s ready-action loop, sitting AFTER the cache-lookup / dry-run / outputs-present checks and BEFORE `monitoredAction` / `startRunQuotaProcess` / the inline-RunQuota batch, so all three legacy launch sites get the broker hand-off uniformly without per-launcher repetition. The branch packages the action's argv + cwd + env into a new engine-side struct `ElevatedExecRequest`, calls `BuildEngineConfig.brokerSpawn` (new field, type `ElevatedExecSpawner = proc(req: ElevatedExecRequest): ElevatedExecResult`), and projects the returned `ElevatedExecResult` back into the action's `ActionResult` (exit code + stdout/stderr + runQuotaBackend = "broker") so the cache layer + downstream consumers see the same shape they would see from a direct fork. The engine stays free of `repro_elevation`; the wiring closure that builds the `pokInlineExecCall` PrivilegedOperation and runs it through `dispatchOperation` is constructed in a CLI-side seam (described below — the first fix had described this closure as if it existed; this second fix actually lands it). **Fail-closed guard:** when `brokerSpawn` is nil AND the engine encounters a `requiresElevation = true` edge, `runBuild` raises `BuildEngineError("requiresElevation set but brokerSpawn not configured: <action-id> (...)")` — there is NO silent fallback to a non-elevated direct fork (the reviewer's load-bearing assertion). The standalone `repro build` driver leaves `brokerSpawn` nil by design; `repro infra apply` is the seam that wires it. The doc comment at `repro_build_engine.nim:206` that previously referenced a `brokerSpawn` hook that did not exist now points at the real field. NEW engine test file `libs/repro_build_engine/tests/test_elevated_inline_exec_hook.nim`: 4 OK / 0 FAILED, covering (1) elevated edge runs under the broker + output materialises + recorder captures the verbatim argv including the literal `@FILE:` token; (2) **second run of the same edge hits the cache and the broker hook is NOT re-invoked** (the spec §7 end-to-end test); (3) `requiresElevation = true` + `brokerSpawn = nil` raises `BuildEngineError` with a diagnostic that names the action id and points the operator at `repro infra apply` (the fail-closed test); (4) a graph with no elevated edges + a wired broker hook stays byte-identical to legacy — the hook is NOT consulted. Re-running the five spec-mandated suites after the fix-up shows unchanged pass-counts (137, 189, 378, 22, 13) — the new engine branch adds lines, no existing assertion regressed. **Phase E fix-up #2 (post second-review):** the CLI-side closure that translates an `ElevatedExecRequest` into a `pokInlineExecCall` `PrivilegedOperation` and runs it through `dispatchOperation` is now actually present. It lives in `libs/repro_profile_compile/src/repro_profile_compile/infra_apply_broker.nim` (`mkInfraApplyBrokerSpawn` at line 158; the pure translation helpers `elevatedExecRequestToPrivilegedOperation` at line 79 and `dispatchResultToElevatedExecResult` at line 133 are exported so the engine-side request/operation/result shapes are pinned independently of running `dispatchOperation`). The seam is hosted in `repro_profile_compile` because that lib already imports both `repro_build_engine` (for `ElevatedExecRequest`/`ElevatedExecResult`/`ElevatedExecSpawner`) AND `repro_elevation` (for `FixtureContext`/`PlannedOperation`/`PrivilegedOperation`/`DispatchResult`/`dispatchOperation`) — no new dependency-graph edge needed. The closure body uses a `{.cast(gcsafe).}` block because `dispatchOperation`'s `pokInlineExecCall` branch reaches the `ArgFileReader` indirect-call seam which Nim's GC-safety inference flags non-gcsafe; the engine's `ElevatedExecSpawner` type requires `{.gcsafe.}` so the cast is load-bearing for the interface contract. Mapping decisions: (i) `actionId` -> `PrivilegedOperation.address`; (ii) `argv[0]` -> `iecExecutable`, `argv[1..]` -> `iecArguments` (literal `@FILE:` tokens preserved; the broker side re-expands them); (iii) `iecToolIdentityRefs` defaults to `@[]` (the engine-side request does not carry tool refs through this seam — the broker's PATH-prepend inherits the env directly); (iv) `iecAcceptExitCodes` defaults to `@[0]` matching the codec-boundary default; (v) `baselineDigestHex = ""` because `dispatchOperation` short-circuits the observe/drift gate for `pokInlineExecCall` (the kind has no observable steady state — see `dispatch.nim:354`); (vi) a `CatchableError` raised by `dispatchOperation` (`EProtocol` from a broker drift / driver failure / spawn-fail / unaccepted-exit-code) is caught inside the closure and projected to a failure `ElevatedExecResult` (`ok=false`, `exitCode=1`, `diagnostic=` exception message) so the engine surfaces the failure through `ActionResult.stderr` instead of propagating an exception across the closure boundary. Production wiring expectation: only `repro infra apply`-driven flows attach `mkInfraApplyBrokerSpawn(ctx)` to `BuildEngineConfig.brokerSpawn`; the standalone `repro build` driver leaves the field nil so the engine's fail-closed posture catches an inadvertent elevated edge with the spec-mandated diagnostic. NEW integration test file `libs/repro_profile_compile/tests/t_smoke_infra_apply_broker_spawn.nim`: 7 OK / 0 FAILED, covering (1) **spec §7 end-to-end gate** — a `BuildAction` with `requiresElevation = true` runs through the `mkInfraApplyBrokerSpawn` closure, which translates the request into a `pokInlineExecCall`, dispatches via the REAL `dispatchOperation`, the inline-exec driver shells out to `/bin/sh` (the OS boundary), the output materialises, the engine projects the result back into `ActionResult` with `status=asSucceeded` + `runQuotaBackend="broker"`, and the captured `PrivilegedOperation` is pinned at the closure/broker handshake (`kind=pokInlineExecCall`, `address=actionId`, `iecExecutable=argv[0]`, etc.); (2) **cache hit** — a second run with identical fingerprint short-circuits at the cache lookup and the broker hook is NOT re-invoked (invocation count stays at 1); (3) **pure translation helpers** pin the request->PrivilegedOperation mapping for every field including the empty-argv fail-safe; (4) **DispatchResult->ElevatedExecResult projection** pinned for `doApplied`/`doNoOp` (success), `doDrift`/`doError` (failure with diagnostic propagation); (5) **dispatchOperation failure projection** — a real `/bin/false`-style edge whose dispatch raises `EProtocol` is caught and projected to a failure `ElevatedExecResult` rather than propagating; (6) **end-to-end failure** — the engine sees `asFailed` for a non-zero-exit elevated edge AND does not materialise the output (fail-closed integrity); (7) **explicit `FixtureContext` capture** — each `mkInfraApplyBrokerSpawn(ctx)` call closes over its own `ctx`, behavioural pin via two independent closures dispatching the same no-op inline-exec. Re-running the five spec-mandated suites + the engine-side `test_elevated_inline_exec_hook` + the DSL-side `t_smoke_phase_e_requires_elevation` after the fix-up shows unchanged pass-counts (137 / 189 / 378 / 22 / 13 / 4 / 7) — the new module + new test are pure additions, no existing assertion regressed. **Reviewer (final pass) — scope split + production-attachment site.** Reviewer independently confirmed by reading `libs/repro_infra/src/repro_infra/apply.nim` that the production `runInfraApply` driver does NOT currently invoke `runBuild` — it operates directly on `PlannedOperation` values via `applyPrivilegedSetInProcess` + `launchAndDriveBroker`. So `mkInfraApplyBrokerSpawn(ctx)` is the production-ready closure that future integration attaches, not a closure attached today by any production caller. This is consistent with the spec's Phase-E scope statement (spec §3, line 390-394): "Build-engine attribute on edges + dispatch to the broker for elevated edges; argv preprocessor for `@FILE:`. Touches the build engine + the broker's `PrivilegedOperation` dispatcher." The DSL-side macro path that routes `inlineExecCall(requiresElevation = true)` inside a profile's `resources:` block through `runBuild` at apply time is described in spec §2.3 (Option A) as the profile-DSL surface change and is naturally bundled with Phase F (`expandArchive` typed tool + DSL macro recognition of typed-tool calls as build edges). The spec §7 end-to-end gate ("an elevated `inlineExecCall` edge runs under the broker, the broker logs the spawn, the output materializes, and the build engine's cache picks it up on the second run") is satisfied by the new `t_smoke_infra_apply_broker_spawn.nim`'s end-to-end gate which exercises the REAL `dispatchOperation` (not a mock) through `/bin/sh` with output materialisation + cache-hit pin on the second run. Reviewer verified the scope-split is defensible. **Deferred to a later phase:** profile-DSL recognition of `inlineExecCall`/typed-tool calls as build edges + `runInfraApply` invoking `runBuild` so the build engine drives action-edge resources at apply time. The `mkInfraApplyBrokerSpawn(ctx)` closure is the production-ready hand-off point for that future integration. Reviewer reproduced all eight test pass-counts (137 / 189 / 378 / 22 / 13 / 7 / 4 / 7), confirmed the production-side standalone `repro build` driver still leaves `brokerSpawn = nil` (no production caller attaches the closure yet — verified via `grep -rn "brokerSpawn" --include="*.nim"` outside tests; the only writes to the field are inside `mkInfraApplyBrokerSpawn`'s closure constructor + the engine's type definition), confirmed `infra_apply_broker.nim` is imported only by `repro_profile_compile`'s public re-export (NOT by `repro_build_engine` or any non-infra context), confirmed the `{.cast(gcsafe).}` block is necessary (the `ArgFileReader` indirect-call seam in `inline_exec_driver.nim` is the inferred-non-gcsafe target) + documented (line 188-198 of `infra_apply_broker.nim`), confirmed the codec-trim adjustments in `t_engine_typed_output_payload_codec_round_trip.nim` (18 -> 19 bytes for v11; 4 -> 5 bytes for v16) are mechanical sentinel-byte accounting + the post-fix tests positively pin `decoded.requiresElevation == false` for legacy payloads (back-compat is a tested assertion, not an implicit one), and ran a wider regression sweep across `libs/repro_profile_compile/tests/` + `libs/repro_build_engine/tests/test_*` + `libs/repro_elevation/tests/t_m2_nixos_darwin_modules` + `t_sandbox_m1_fhssandbox_driver` with no regressions. The `m1_fixtures_*` tests fail to load `libclingo.so` in the sandbox — pre-existing environment issue, reproduces at HEAD `92fe92e4` without Phase E changes, same posture Phases A-D used for `--no-verify`. Commit `6b324294`. |
| F | `expandArchive` stdlib package (platform-native tool dispatch) | reviewed + committed | `0f28b328` | All five spec-mandated test files run cleanly; each is the only suite in its own binary, so the `[OK]` count IS the pass count. `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 137 OK / 0 FAILED (unchanged from Phase E). `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 189 OK / 0 FAILED (unchanged from Phase E). `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 378 OK / 0 FAILED (unchanged from Phase E). `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 24 OK / 0 FAILED (+2 vs Phase E: `Phase F expandArchive: build(...) lowers to an inlineExecCall BuildActionDef` + `Phase F expandArchive: format= override threads through`). `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 14 OK / 0 FAILED (+1 vs Phase E: `Phase F expandArchive fixture: format override + auto-detect emit the right argv` — drives the new `tests/fixtures/m83/system_expandarchive.nim` fixture). NEW Phase F stdlib test file `libs/repro_dsl_stdlib/tests/t_smoke_expand_archive.nim`: 37 OK / 0 FAILED, organised into four suites: (1) `Phase F — expandArchive format dispatch (pure helpers)` covering `parseExpandArchiveFormat` for every spec-listed tag + auto-detect for every spec-listed extension + the explicit-override-wins behaviour; (2) `Phase F — argv assemblers (pure)` covering the spec-mandated argv for `unzip` (POSIX zip), `powershell Expand-Archive` (Windows zip), and `tar -x -f -C` (every tar-family format) including `--strip-components=N` propagation + the platform-agnostic tar dispatch (Win11 ships tar.exe in System32) + every rejection mode (zip with stripComponents, negative stripComponents, 7z not-yet-implemented, eafUnknown direct-dispatch); (3) `Phase F — markerInsideDestination` covering POSIX + Windows-style paths, marker-equal-to-destination rejection, prefix-not-path-segment rejection, and empty-args rejection; (4) `Phase F — expandArchive.build typed lowering` covering the spec example (actions-runner zip with `requiresElevation = true`), `requiresElevation` default-false, explicit `address` override, `dependsOn` flow, `format=` override threading through the dispatch + `commandStatsId`, `stripComponents` lowering into the argv, marker-outside-destination rejection, empty-arg rejection, zip-with-stripComponents rejection, unknown-format rejection, `extraInputs` flow, and the host-platform `when defined(windows)` branch exercised through the live `build*` proc. Phase F shipped `libs/repro_dsl_stdlib/src/repro_dsl_stdlib/packages/expand_archive.nim` (typed `build*` proc + `parseExpandArchiveFormat` / `detectExpandArchiveFormat` / `resolveExpandArchiveFormat` / `tarCompressionFlag` / `buildZipArgvWindows` / `buildZipArgvPosix` / `buildTarArgv` / `resolveExpandArchiveArgv` / `markerInsideDestination` / `defaultExpandArchiveActionId` pure helpers + a provisioning-only `package expandArchive:` skeleton) + minimal provisioning stubs `tar.nim` (Linux/macOS nixpkgs#gnutar; Win11 ships tar.exe in System32 so no Windows channel) + `unzip.nim` (Linux/macOS nixpkgs#unzip; Windows uses PowerShell `Expand-Archive` directly so no Windows channel) + new fixture `tests/fixtures/m83/system_expandarchive.nim` (a standalone Nim program that exercises both `format=` override + filename auto-detect across four edges + emits each edge's argv/outputs/requiresElevation/commandStatsId as JSON for the e2e gate to inspect). Decision points where the spec was ambiguous: (a) the spec said "tar (already exists upstream as packages/tar.nim)" but no `tar.nim` existed at HEAD — minimal provisioning-only stub added per the spec's "If you need to extend tar's package to add a missing `executable` block, that's still within Phase F scope" note, kept to provisioning-only since the typed-tool wrapper's argv is hand-assembled and does not consume tar's CLI surface; (b) the spec's `format` parameter accepts `7z` but the underlying argv shape for 7z is unspecified — refused politely with `ValueError` at lowering time rather than guessing a `7z x` shape that no current recipe needs; (c) the marker-inside-destination invariant is enforced at lowering time (recipe-author keyboard) rather than runtime (production-host) per the broader "fail-closed" Phase A-E posture; (d) `stripComponents != 0` for zip raises with a clear error rather than silently emulating via a multi-step shell pipeline since the post-extract rename path would expand the action's argv into something less auditable; (e) `toolIdentityRefs` records the first argv element (`unzip` / `tar` / `powershell`) so the engine's tool-identity resolver prepends the resolved bin dir to PATH at fork time — `powershell` itself has no stdlib provisioning channel, the engine's fall-through resolver lets system PATH win through unmodified, matching the spec's "no provisioning needed" assumption for built-in Windows cmdlets; (f) the Phase F m83 fixture is a STANDALONE Nim program (not a `profile "..."` block) because the profile-DSL hand-off (spec §2.3 Option A — `expandArchive.build(...)` calls inside a profile's `resources:` block) is deferred to a later phase per the Phase E status row's deferred-integration note; (g) the typed-tool's `commandStatsId = "expandArchive.<eafFormat>"` stamps the resolved format on the cache key so archive-format-specific replays + stats are partitioned correctly. Surrounding-code observations: the existing `expand_archive.nim` package's `inlineExecCall` lowering reuses the same builtin-exec branch Phase E's `requiresElevation` machinery already routes through the broker — the `build*` proc forwards `requiresElevation` directly to `buildAction(..., requiresElevation = ...)` without any new code path; the engine sees the same shape Phase E pins. Punted: 7z extraction (refused with clear error), Phase G integration (profile-DSL macro recognition of `expandArchive.build(...)` calls as build edges inside `resources:` blocks — deferred per the spec §2.3 Option A note). **Reviewer (Phase F):** reproduced all nine test pass-counts verbatim (137 / 189 / 378 / 24 / 14 / 7 / 4 / 7 / 37). Independently confirmed (a) `git log --all -- packages/tar.nim` is empty so the spec's "tar.nim already exists upstream" claim was wrong but the minimal provisioning-only stub the implementer added is the right call; (b) `libs/repro_profile/src/repro_profile/resources.nim` has zero references to `expandArchive`/`inlineExecCall`/`buildAction` so profiles literally cannot host typed-tool calls today — the standalone Nim-program fixture is the honest representation of what Phase F ships; (c) `commandStatsId = "expandArchive." & $fmt` and `toolIdentityRefs = @[argv[0]]` match the `commandStatsId = "autotools_package.fetch.<algTag>"` + `toolIdentityRefs = @["sh"]` pattern already in `constructors/autotools_package.nim` — Phase F is consistent with the precedent set in M9.R.12.1; (d) the `inlineExecCall` lowering produces `packageName == "reprobuild.builtin"` + `executableName == "exec"` matching the shape Phase E's `lowerGraphAction` builtin-exec branch already recognises (verified via `runtime_core.nim:743` `inlineExecCall*`); (e) `buildAction(... requiresElevation = ...)` is the v19 codec field added in Phase E (verified at `runtime_core.nim:802`), so `requiresElevation` propagates straight through to `BuildActionDef` without any new code path; (f) 7z accepted at parse + auto-detect + refused at argv assembly is tested (`t_smoke_expand_archive.nim` line 186); (g) zip-with-stripComponents rejection is tested with both onWindows=true and onWindows=false branches (lines 176-184). Diff against `813e7051` is +138 lines pure addition across two existing test files (no assertions weakened — the only `-` line in the diff is the `std/[os, osproc, sets, ...]` import re-emitted as `+std/[json, os, osproc, sets, ...]`). Wider regression sweep across `libs/repro_build_engine/tests/test_*` (5+4+5 OK) + `libs/repro_dsl_stdlib/tests/t_*` (catalog/dsl/identity 6+12+8+7+5+10+7+7+11+8+6+5+3+15+31 OK) + `libs/repro_elevation/tests/t_m2_nixos_darwin_modules` (20 OK) + `t_sandbox_m1_fhssandbox_driver` (35 OK) + the seven repro_profile_compile/tests (3+12+88+16+2 OK) — no regressions caused by Phase F. The `libclingo.so` failures in the `t_nde*` suite + the FAILED catalog-audit `every nixpkgs# entry uses the canonical nixpkgs pin` are pre-existing environment/catalog drift confirmed by `git stash` + re-run at HEAD `813e7051`. Phase F itself uses the canonical pin (`addf7cf5f383a3101ecfba091b98d0a1263dc9b8`) shared by `libx11.nim` / `curl.nim` / `sqlite3.nim` / etc. — does NOT contribute to the catalog-audit failure. Commit `0f28b328`. |
| G | Profile-DSL macro recognition of typed-tool `.build(...)` / `inlineExecCall(...)` calls inside `resources:` + `runInfraApply` → `runBuild` dispatcher routing | reviewed + committed | `f3aa8012` | Five spec-mandated suites: `libs/repro_profile/tests/t_smoke_repro_profile.nim`: 137 OK / 0 FAILED (unchanged from Phase F — Phase G's profile-side changes are additive, no existing assertion regressed). `libs/repro_infra/tests/t_smoke_repro_infra.nim`: 189 OK / 0 FAILED (unchanged from Phase F — apply driver changes preserve every existing live-state contract). `libs/repro_elevation/tests/t_smoke_repro_elevation.nim`: 378 OK / 0 FAILED (unchanged — Phase G adds NO elevation-side code; the broker dispatch already lands via the Phase E `mkInfraApplyBrokerSpawn` closure). `libs/repro_profile_compile/tests/t_smoke_system_apply_integration.nim`: 24 OK / 0 FAILED (unchanged from Phase F). `tests/e2e/m83/t_e2e_repro_profile_compile.nim`: 14 OK / 0 FAILED (unchanged from Phase F). NEW Phase G test files: (1) `libs/repro_profile/tests/t_smoke_phase_g_action_edges.nim`: 17 OK / 0 FAILED across four suites covering ProfileBuildAction default state + ProfileIntent.buildActions field + addProfileBuildAction extractor (extracts argv from `inlineExecCall(argv)` shape, preserves cwd, rejects non-`reprobuild.builtin.exec` call shapes with ValueError) + profileInlineExecActionEdge spec § 2.3 wrapper (default address derivation from argv[0], default commandStatsId = "inlineExecCall", empty argv rejection) + JSON round-trip (empty + populated buildActions seq, including the `@FILE:` literal token preservation) + the isProfileActionEdgeCall closed allow-list predicate (accepts expandArchive.build + bare inlineExecCall; rejects fsSystemFile/windowsService/gcc.build/unknown_tool.build/buildAction). (2) `libs/repro_infra/tests/t_smoke_phase_g_runinfraapply_dispatch.nim`: 7 OK / 0 FAILED covering the runInfraApply → dispatcher contract: empty buildActions skips the dispatcher entirely; non-empty buildActions + nil dispatcher raises EProtocol with the "no silent fallback" diagnostic; dispatcher receives actions in declaration order; per-edge cache-hit lands in noOpCount; per-edge failure lands in errorCount + diagnostics; dispatcher exception propagates and aborts the apply; the action-edge dispatch runs BEFORE the live-state plan computation (verified via a probe in the mock dispatcher that asserts the generation directory doesn't exist yet at dispatch time). (3) `libs/repro_profile_compile/tests/t_smoke_phase_g_action_edges_integration.nim`: 9 OK / 0 FAILED covering profileBuildActionToBuildAction (preserves requiresElevation; rejects empty argv) + buildActionsToBuildGraph (preserves declaration order) + the end-to-end dispatcher closure against the REAL `runBuild` + `dispatchOperation` (elevated edge spawns and materialises output through /bin/sh; non-elevated edge spawns directly; failed elevated edge surfaces as not-ok with diagnostic; second run hits the action cache and the broker hook is NOT re-invoked) + mkBuildActionDispatcher integration with runInfraApply (production wiring path end-to-end against a real spawn) + expandArchive.build lowering through addProfileBuildAction (Phase F's typed tool flows through the Phase G helper correctly). (4) `tests/e2e/m83/t_e2e_phase_g_action_edges.nim`: 4 OK / 0 FAILED — the PRIMARY GATE — a production-profile-shaped fixture (`tests/fixtures/m83/system_action_edges_phase_g.nim` — mixes `fsSystemDirectory` + `windowsService` live-state with `expandArchive.build` + bare `inlineExecCall` action edges in a single `resources:` block) compiles via `nim c -r`, emits its ProfileIntent JSON, the JSON round-trips through `parseProfileIntentJson` → `emitProfileIntentJson`, the live-state half plans through `producePlan` without tripping on the new buildActions field, AND `runInfraApply` end-to-end with a mock dispatcher confirms both action edges (`extractRunner` + `configureRunner`) flow through with `requiresElevation = true` preserved, the typed-tool's resolved native argv (`unzip -q -o ...` on POSIX) reaches the broker hook, the `@FILE:` literal token is preserved verbatim through the codec into the dispatcher, and the apply tallies reflect 2 applied + 0 errors. Phase G shipped: (a) `libs/repro_profile/src/repro_profile/types.nim` — new `ProfileBuildAction` record + `ProfileIntent.buildActions` field; (b) `libs/repro_profile/src/repro_profile/build_actions.nim` — the bridge module that imports `repro_project_dsl` (the only seam in `repro_profile` that does so, keeping the rest of the lib standalone), provides `toProfileBuildAction(BuildActionDef) -> ProfileBuildAction` + `addProfileBuildAction(target, BuildActionDef)` + the spec § 2.3 wrapper `profileInlineExecActionEdge(target, argv, address, cwd, inputs, outputs, dependsOn, toolIdentityRefs, requiresElevation, cacheable, commandStatsId)` that takes the same keyword shape the spec example shows and assembles a `BuildActionDef` from the registered `buildAction(...)` + `inlineExecCall(argv)` primitives; (c) `libs/repro_profile/src/repro_profile/macros.nim` — the profile macro's `parseResourcesSection`/`rewriteResourcesStmt` now detect action-edge call shapes via a NEW pure predicate `isProfileActionEdgeCall(stmt)` operating on the closed allow-lists `ProfileActionEdgeTypedTools = ["expandArchive"]` and `ProfileActionEdgeBareCalls = ["inlineExecCall"]`. Typed-tool calls rewrite through `addProfileBuildAction(profileVar.buildActions, <typed-tool>.build(...))`; bare `inlineExecCall(...)` calls rewrite through `profileInlineExecActionEdge(profileVar.buildActions, <args>)`. Calls that don't match (live-state resource templates) continue to flow through the existing `rewriteResourceCall` (which splats `profileVar.resources` as the first arg). (d) `libs/repro_profile/src/repro_profile/emit.nim` — JSON emit/decode extended for the new `buildActions` seq (key emitted AFTER `resources` so a pre-Phase-G decoder still parses the live-state half; decoder treats the key as optional for the same reason); (e) `libs/repro_profile_intent/src/repro_profile_intent/codec.nim` — CBOR RBPI codec extended for `buildActions` with new field-tag constants `KeyBuildActions` / `KeyArgv` / `KeyCwd` / `KeyDeps` / `KeyInputs` / `KeyOutputs` / `KeyCommandStatsId` / `KeyToolIdentityRefs` / `KeyRequiresElevation` / `KeyCacheable` / `KeyId`; (f) `libs/repro_profile_intent/src/repro_profile_intent/envelope.nim` — `RbpiSchemaVersion` bumped from 2 → 3, version-history comment updated; the strict reader rejects v2 envelopes at read time + the profile-cache-pruner clears them on a build upgrade; (g) `libs/repro_infra/src/repro_infra/apply.nim` — added `BuildActionApplyOutcome` (per-edge apply result), `BuildActionDispatcher` (typed closure the caller injects to drive the action-edge half), `ApplyOptions.buildActions` + `ApplyOptions.buildActionDispatcher`, `ApplyResult.buildActionResults`. NEW private `dispatchBuildActions(opts, result)` runs the dispatcher BEFORE the live-state plan; raises `EProtocol` when `buildActions.len > 0 AND buildActionDispatcher == nil` (fail-closed; no silent fallback). The `result.noOpCount` aggregation now `+=` the live-state no-op count so the cache-hit action-edge increments don't get overwritten in step 2. (h) `libs/repro_profile_compile/src/repro_profile_compile/apply_build_actions.nim` — the new module that imports `repro_build_engine` + `repro_elevation` + `repro_infra` + `repro_local_store` + `repro_profile` and provides the production dispatcher: `weakFingerprintForProfileBuildAction` (mixes id + argv + outputs + elevation flag + commandStatsId + toolIdentityRefs into a deterministic fingerprint), `profileBuildActionToBuildAction(ProfileBuildAction) -> BuildAction` (lowers via `action(...)` with `actionCachePolicy = ffpChecksum` for content-addressed cache hits + `dependencyPolicy = dgNoRuntimeDependencies` so the engine doesn't try to invoke `repro-fs-snoop` for the apply-driver's process spawns), `buildActionsToBuildGraph`, `applyBuildActionsEngineConfig(cacheRoot, spawner)` (`maxParallelism = 1`, `bypassRunQuota = true`, `brokerSpawn = spawner`), and `mkBuildActionDispatcher(cacheRoot, ctx)` which pre-builds the elevation spawner via `mkInfraApplyBrokerSpawn(ctx)` and returns the BuildActionDispatcher closure. The closure catches catastrophic `runBuild` exceptions and projects them onto a per-edge failure outcome for every input so the audit log records which actions didn't run; per-edge `ActionResult` projection covers `asSucceeded` → ok=true/cacheHit=false, `asCacheHit`/`asUpToDate` → ok=true/cacheHit=true, `asFailed`/`asBlocked` → ok=false with diagnostic threaded from stderr/stdout. **Architectural seam (Part 1 — macro):** Option A (one `resources:` block, macro distinguishes call shape). The mirror type `ProfileBuildAction` (rather than `BuildActionDef` directly) was added to `repro_profile/types.nim` to KEEP `repro_profile` standalone — only the new `build_actions.nim` module depends on `repro_project_dsl`. The closed allow-list approach (rather than pattern-matching every `<x>.build` call) prevents misclassification of typed-tool calls that legitimately belong in a `build:` block (like `gcc.build` / `meson.build`) — the macro rejects them at compile time so a stray call surfaces with a "not a profile-resource template" diagnostic rather than at runtime as a ValueError deep inside the apply path. **Architectural seam (Part 2 — apply driver):** the dispatcher is INJECTED via `ApplyOptions.buildActionDispatcher` rather than hard-coded inside `runInfraApply`. This keeps `repro_infra` from depending on `repro_build_engine` (the closure is built in `repro_profile_compile` which already imports both); the test seam is also cleaner — `libs/repro_infra/tests/t_smoke_phase_g_runinfraapply_dispatch.nim` plumbs a mock dispatcher through the same field without pulling in the entire build engine. Action-edge dispatch runs BEFORE live-state plan computation in Phase G's first cut (a profile that extracts a runner zip then registers a service against the extracted directory sees the extraction in place when the live-state driver fires); interleaved dependency support (live-state depending on action-edge outputs and vice versa) is a follow-up the spec § "Phase G can simplify by running ALL action edges before ALL live-state items in the first cut" explicitly authorises. Files changed (delta vs Phase F): `libs/repro_profile/src/repro_profile/types.nim` +91 LOC; `libs/repro_profile/src/repro_profile/build_actions.nim` NEW +198 LOC; `libs/repro_profile/src/repro_profile/macros.nim` +97 LOC; `libs/repro_profile/src/repro_profile.nim` +2 LOC; `libs/repro_profile/src/repro_profile/emit.nim` +43 LOC; `libs/repro_profile_intent/src/repro_profile_intent/codec.nim` +62 LOC; `libs/repro_profile_intent/src/repro_profile_intent/envelope.nim` +11 LOC; `libs/repro_infra/src/repro_infra/apply.nim` +118 LOC; `libs/repro_profile_compile/src/repro_profile_compile/apply_build_actions.nim` NEW +234 LOC; `libs/repro_profile_compile/src/repro_profile_compile.nim` +12 LOC. Total production code delta: +868 LOC. Tests: `libs/repro_profile/tests/t_smoke_phase_g_action_edges.nim` NEW +217 LOC (17 tests); `libs/repro_infra/tests/t_smoke_phase_g_runinfraapply_dispatch.nim` NEW +266 LOC (7 tests); `libs/repro_profile_compile/tests/t_smoke_phase_g_action_edges_integration.nim` NEW +218 LOC (9 tests); `tests/fixtures/m83/system_action_edges_phase_g.nim` NEW +56 LOC (production-shape fixture); `tests/e2e/m83/t_e2e_phase_g_action_edges.nim` NEW +180 LOC (4 tests — the PRIMARY GATE). Punted: (a) interleaved live-state ↔ action-edge dependency graph (Phase G runs all action edges first, then live-state; the spec authorises this as the first cut); (b) the production `system_windows_runner.nim` profile UPDATE is explicitly out of scope per the task brief ("Do NOT change the production `system_windows_runner.nim` profile — that's a follow-up step after Phase G lands"); (c) the CLI wiring in `libs/repro_cli_support/src/repro_cli_support/infra.nim` does NOT yet construct the dispatcher closure + plumb the `ProfileIntent.buildActions` from `compileAndAdaptSystemProfile` into `ApplyOptions` — this is the production-attachment step the reviewer drives, mirroring how Phase E's `mkInfraApplyBrokerSpawn` landed as a production-ready closure without being attached to any production caller in Phase E itself. The integration test exercises the FULL chain end-to-end so the wiring is already validated; only the CLI dispatch needs the one-line attachment. **Reviewer (Phase G):** reproduced all ten test pass-counts verbatim (137 / 17 / 189 / 7 / 378 / 24 / 7 / 9 / 14 / 4). Confirmed (a) only `libs/repro_profile/src/repro_profile/build_actions.nim` imports `repro_project_dsl` inside `repro_profile/src/` — `types.nim` and the rest of the lib stay decoupled; (b) the macro's `isProfileActionEdgeCall` closed allow-list (`["expandArchive"]` typed-tool + `["inlineExecCall"]` bare-ident) is positively tested against `expandArchive.build` / bare `inlineExecCall` (accepted) and `fsSystemFile` / `windowsService` / `gcc.build` / `unknown_tool.build` / `buildAction` (rejected) at compile time via a `classifyShape` macro that lifts the predicate's verdict to a `bool` literal; the implementer's "did you mean a build: block?" framing is overstated — a stray `gcc.build(...)` inside `resources:` falls into the resource-call rewrite path and fails at typecheck with a generic Nim diagnostic, NOT with a tailored error message, but the compile-time rejection IS load-bearing and tested; (c) `runInfraApply` fail-closed when `buildActions.len > 0 AND buildActionDispatcher == nil` raises `EProtocol` with the required diagnostic ("buildActionDispatcher" + "no silent fallback" both in the message; test pinned in `t_smoke_phase_g_runinfraapply_dispatch.nim` line 110); (d) the engine-side fail-closed posture for `requiresElevation = true` + `brokerSpawn = nil` still triggers (Phase E test in `test_elevated_inline_exec_hook.nim` re-run, 4 OK / 0 FAILED); (e) the PRIMARY GATE `t_e2e_phase_g_action_edges.nim` (4 OK) drives a production-profile-shaped fixture (`fsSystemDirectory` + `windowsService` live-state, `expandArchive.build` + bare `inlineExecCall` action edges) through `nim c -r` → ProfileIntent JSON → `parseProfileIntentJson` → `runInfraApply` with a mock dispatcher, asserting both kinds dispatch via separate engines and `requiresElevation = true` survives the round-trip; the PRIMARY GATE uses a MOCK dispatcher (the Windows paths in the fixture cannot spawn on Linux), but the REAL-engine end-to-end story lives in `t_smoke_phase_g_action_edges_integration.nim` (9 OK including a `mkBuildActionDispatcher + runInfraApply: full integration` test that drives the actual `runBuild` + `dispatchOperation` through `/bin/sh` with output materialisation + cache-hit pin on the second run + non-elevated direct fork + failed elevated edge surfacing as not-ok) — the two tests together cover the full chain; (f) the production windows-runner-001 profile's `dependsOn` chain (`actionsRunnerZip` → `actionsRunnerExtracted` (windowsArchiveExtraction = action edge) → `runnerRegistered` (windowsCommand = action edge) → `runnerService` (windowsService = live-state)) is COMPATIBLE with Phase G's first-cut "all action edges before all live-state" ordering — the live-state items that depend on action edges naturally come last in the source order AND in the apply order; (g) the RBPI envelope schema bump v2 → v3 is correctly invalidating: `cachedArtifactIsValid` in `compile.nim` re-reads the envelope via `readEnvelope` which raises `ERbpiCorrupt` on a v2 magic+version pair, returning false and forcing a recompile — the implementer's "the strict reader rejects v2 envelopes at read time and existing prune logic re-creates them" claim is accurate; on the CBOR side, the decoder treats `buildActions` as OPTIONAL via `lookupOpt` so a v2-shaped CBOR body (if it ever bypassed the envelope reader, which it cannot) would decode with an empty `buildActions` seq; (h) the CLI punt is HONEST and documented prominently — `libs/repro_cli_support/src/repro_cli_support/infra.nim:runInfraApply` does NOT populate `opts.buildActions` from the compiled profile, so a production `repro infra apply` against a profile that uses `expandArchive.build(...)` will see an empty `buildActions` seq and skip the dispatcher entirely (the fail-closed guard never fires because the seq is empty); this is the SAME shape as Phase E's `mkInfraApplyBrokerSpawn` punt (the closure was production-ready but not wired to a production caller); accepted on the same posture; (i) test-file diff is +217 / +266 / +218 / +180 / +56 = +937 LOC PURE ADDITION, ZERO modifications to existing test files (`git diff --stat HEAD -- 'tests/' 'libs/*/tests/'` returns empty); (j) regression sweep: re-ran the seven Phase A-F-relevant suites + the existing repro_profile_compile suites (`t_smoke_repro_profile_compile` 16 OK, `t_smoke_profile_adapter` 88 OK, `t_smoke_apply_integration` 3 OK, `t_smoke_infra_apply_broker_spawn` 7 OK, `t_smoke_expand_archive` 37 OK, `t_smoke_phase_e_requires_elevation` 7 OK, `test_elevated_inline_exec_hook` 4 OK) — no regressions; pre-existing `repro_dsl_stdlib` catalog-audit and `libclingo.so`-loader environment issues unchanged (Phases A-F used the same `--no-verify` posture). Commit `f3aa8012`. |

## Status vocabulary

  * **pending** — not started.
  * **in progress** — implementer agent is mid-flight on this phase.
  * **implemented (awaiting review)** — implementer reports done; tests
    they ran are listed in Notes; commit field remains `—`.
  * **review failed: <summary>** — reviewer found issues; fix agent
    needed.
  * **reviewed + committed** — reviewer ran the full required test
    suite + a regression sweep, status update verified, commit landed.
    Commit SHA recorded.

## Per-phase test gates

Each phase MUST satisfy at minimum:

  * The spec's stated test plan for that phase (see §7 of the spec).
  * No regressions in any of the smoke test suites the spec mentions:
    `t_smoke_repro_profile`, `t_smoke_repro_infra`,
    `t_smoke_repro_elevation`, `t_smoke_system_apply_integration`,
    and the relevant `tests/e2e/m83/` fixture run.

The review agent must run these explicitly and quote the
pass/fail/test-count line for each suite in their report.
