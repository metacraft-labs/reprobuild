# cbor

Minimal deterministic CBOR subset used by the M6 integration gate for dynamic
metadata fields. It supports null, booleans, unsigned integers, byte strings,
text strings, arrays, and string-keyed maps with canonical key ordering.

This is not the long-term production CBOR stack.
