# repro_monitor_hooks

Hook backend adapter layer for the extracted `ct_interpose` monitor substrate.

M10 exposes the collector/finalizer entry point used by the macOS shim
integration gate. Backend selection and user-facing CLI work remain future
milestones.
