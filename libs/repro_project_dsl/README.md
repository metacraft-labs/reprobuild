# repro_project_dsl

Minimal M7 project DSL surface used to lift package executable CLI interfaces
from real Nim modules. This is intentionally narrow and covers the package /
executable / cli shape exercised by the interface artifact integration tests.
