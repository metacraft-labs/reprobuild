version       = "0.1.0"
author        = "Metacraft Labs"
description   = "HistoryReporter: the parallel test runner's write path into RunQuota's observation store (ext_test_execution + ext_codetracer_test)"
license       = "MIT"
srcDir        = "src"

requires "nim >= 2.2.0"
requires "ct_test_interface >= 0.1.0"
