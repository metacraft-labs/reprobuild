## Compile-time switch enabling the provider-mode-only ``buildPackageFragment``
## proc from ``runtime_provider.nim``. The test exercises
## ``conv.emitFragment(...)`` directly, which calls into
## ``buildPackageFragment`` from inside ``from_source_meson.nim``.
## Without this define the proc is gated behind
## ``when defined(reproProviderMode):`` and the convention's reference
## to it is undeclared.
--define:reproProviderMode
