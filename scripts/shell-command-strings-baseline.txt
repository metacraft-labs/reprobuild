# shell-command-strings baseline — see scripts/check_shell_command_strings.sh
# Regenerate with: scripts/check_shell_command_strings.sh --write-baseline
# Then WRITE THE VERDICT for each entry: it is a defect until argued otherwise.
#
# Format: <file>|<exec api>|<operator classes>|<the offending literal(s)>
# Line numbers are deliberately absent so an edit above a site does not
# invalidate the baseline.
#
# Note what is NOT here. Three sites in the tree carry a redirect token and are
# provably shell-bound, so the scanner exempts them STRUCTURALLY rather than by
# baseline — that is the judgement this check exists to stop people
# re-deriving:
#   - the dev-session supervisor detach (`) </dev/null >> … 2>&1 &`) goes
#     through `execShellCmd`, which is a real shell on both platforms;
#   - the `setsid` cache-push detach lives in the `of wtPosix:` arm of a
#     `case targetOs`, and the `wtWindows` arm builds an argv;
#   - `runFixtureCmd` appends `>'…' 2>&1` to an explicit `sh -c` BODY with
#     `sh` as argv[0].
# Break any of those three properties and the site appears here.

# EMPTY ON PURPOSE, as of W11/W12/W13 (2026-09-13). Both entries this file
# carried were the SAME family and both are fixed, so the list of sites that
# already carry the defect is now the empty list:
#
#   - `typecheckConsumerAgainstInterface` built `"cd " & quoteShell(repoRoot) &
#     " && " & cmd` for `execCmdEx` and RAISED on Windows instead of returning
#     a verdict (W12). It now passes `workingDir` to an argv `startProcess`
#     (`runArgvCapturingInDir`). Its only caller,
#     `t_rp8_lsp_typecheck_and_goto_def`, carried a TEST-SIDE instance of the
#     same family that masked it, and that one is fixed too -- fixing either
#     alone leaves the case red on Windows.
#   - the `zekZstdPipe` extractor built `zstd -dc <archive> | tar -xf - -C
#     <dest>` for `execCmdEx` under a comment asserting that cmd.exe honours
#     the `|` (W13). The pipe is now connected in-process and the comment says
#     what actually happens.
#
# Keep it empty. A candidate that is not listed here fails the check, which is
# now the whole first-party production scope: every `execCmdEx` / `execProcess`
# command string in `libs`, `apps`, `tools` and `repro.nim` is free of shell
# metacharacters, and the three structurally-exempt sites above are the only
# redirect tokens left in it.
